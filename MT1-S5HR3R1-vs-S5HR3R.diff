diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=test-results' '--exclude=*.zip' '--exclude=*.diff' '--exclude=*.Zone.Identifier' MT1-S5HR3R/MT1_S5HR3R1_CLOSURE.md MT1-S5HR3R1/MT1_S5HR3R1_CLOSURE.md
--- MT1-S5HR3R/MT1_S5HR3R1_CLOSURE.md	1969-12-31 18:00:00.000000000 -0600
+++ MT1-S5HR3R1/MT1_S5HR3R1_CLOSURE.md	2026-08-26 19:39:06.947252717 -0500
@@ -0,0 +1,21 @@
+# MT1-S5HR3R1 — Final V1 Proof-Identity Migration Closure
+
+1. **Source/new hashes:** source `MT1-S5HR3R.zip` is `072d936e4bef77c097b464f6aaf414feb630cafaba6a533f9dfbb9e5c4256018`; the new `MT1-S5HR3R1.zip` hash is recorded in `MT1-S5HR3R1-SHA256SUMS.txt` after deterministic packaging.
+2. **Frozen contract hashes:** `MT1_S5_AUTHORITY_CONTRACT_V1.md` remains `e15885cc11777ad49f2b0b5ad2778facb64ee6f932b65bf36742a65afe451ba9`; `MT1_S5_FROZEN_NEGATIVE_CONTROL_CONTRACT.md` remains `8b150937b002d7a054e098182d6453711c477f8eaeb1865c1663541dc135f70d`. Neither contract changed.
+3. **Nominal geometry mutation:** **NO**. `src/design/s5Parameters.ts` remains byte-identical at `8d713b182f94af2769bec0696fb10cfc94fce98d90e89a46a69aad68e805fc63`; B′, S4A, machine maps, can/core/spigot, shoes, pins, rails, receivers, shoulders, and backing geometry are unchanged.
+4. **S5 proof-semantic `getMeshByName`:** **0 remaining**. Pin extension and retained/reverse/escape probes resolve the required registered pin rows through frozen semantic bindings, then manipulate those exact rows by registration ID.
+5. **Support graph:** registration ID is the key type for rail/backing/post adjacency, edge identity, BFS start/goal/seen/queue, and continuity. Nominal result is PASS with 70 ID-keyed edges, zero missing relationships, and report-only display names carried beside `fromRegistrationId`/`toRegistrationId`.
+6. **Supersession/material migration:** post-registration supersession, live census material scope, and S5 classification use `getSupersededByRegistrationId`, `getS5ClassByRegistrationId`, and `isS5MaterialRegistration`. Historical S1–S4 semantics were not changed.
+7. **Remaining forbidden proof-semantic name uses:** **0** in the bounded S5 authority/proof audit. Static regressions reject live-name pin selection, name-keyed support traversal, and name-keyed material/supersession scope.
+8. **Allowed name uses:** names remain human-readable report/diagnostic labels and frozen semantic-role declarations. A one-time semantic-name→registration-ID resolution is allowed only after global identity/name uniqueness passes; it does not govern later physical proof identity.
+9. **Identity-preflight ordering:** `auditS5AuthorityIdentity()` runs before required-topology binding, world/census projection, support/load projection, passive contact simulation, or any required-solid probe. A failed preflight produces an invalid certificate/readiness result immediately.
+10. **Duplicate-name fail-before-projection:** PASS. `NC-DUPLICATE-AUTHORITY-NAME` preserves both conflicting registration IDs, reports `namesUnique=false`, evaluates zero path samples and zero proof pairs, leaves topology/support/load/contact projections empty, fails C18/certificate, and makes raw/evaluator/getter readiness false.
+11. **V2:** **CLOSED**. The immutable exact-registration-ID contact table remains the sole S5 intentional-contact authority: nine rule classes, 223 instantiated relations, zero missing relations.
+12. **V3:** **CLOSED**. The wrong-sector exact registered pair remains unlisted and fails ordinary collision handling, C18, the certificate, and all readiness APIs; nominal same-sector relations remain valid.
+13. **Frozen negative controls:** **28/28 PASS**, including `NC-MIDPATH-TRANSIENT-ROW`, `NC-DUPLICATE-AUTHORITY-NAME`, and `NC-DUPLICATE-MOVING-ROW`. No frozen control was added or removed. `unexpectedFailures` total is 0 and four-way raw/evaluator/getter/certificate restoration is 28/28.
+14. **P/C results:** P1–P16 **16/16 PASS**; C1–C25 **25/25 PASS**. No C26 or new freeze gate exists. Nominal certificate: 394 registered rows, 365 physical rows, 95 lifetime-relevant rows, exact topology 98/98, 2,002 samples, 2,981,632 pair evaluations, and 56,112 moving↔moving evaluations.
+15. **Build/tests/evidence/S4:** build PASS; focused HR3R1 suite 7/7 PASS in 9.9 minutes; supported exact-state `npm test` 95/95 PASS in 28.5 minutes; authority writer 1/1 PASS in 3.6 minutes; atomic 28-row evidence writer 1/1 PASS in 43.4 minutes; strict ledger validator 1/1 PASS; bounded S4 controls 6/6 PASS in 5.0 minutes. The readiness transition remains false at 0.99982 and first stable true at 0.99983.
+16. **H1:** `AWAITING_DIRECTOR_DISPOSITION`; it was not self-passed.
+17. **Limitations:** this proves the frozen registered S5 universe and frozen 28-control boundary, not arbitrary future Babylon mutations, universal rigid-body dynamics, friction/preload/springs, stress/fatigue/tolerance/manufacturing, propulsion performance, or any S6/cockpit/intake/engine/aero scope. No post-S5 hardening candidate was discovered in this bounded pass.
+
+Disposition: **FREEZE_CANDIDATE_PENDING_DIRECTOR** / authority **GREEN_PENDING_DIRECTOR**. STOP after HR3R1; S6 was not begun.
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=test-results' '--exclude=*.zip' '--exclude=*.diff' '--exclude=*.Zone.Identifier' MT1-S5HR3R/MT1_S5_AUTHORITY_REPORT.md MT1-S5HR3R1/MT1_S5_AUTHORITY_REPORT.md
--- MT1-S5HR3R/MT1_S5_AUTHORITY_REPORT.md	2026-08-25 16:29:47.000000000 -0500
+++ MT1-S5HR3R1/MT1_S5_AUTHORITY_REPORT.md	2026-08-26 19:38:39.466642497 -0500
@@ -1,26 +1,28 @@
-# MT1-S5HR3R — Authority Report
+# MT1-S5HR3R1 — Authority Report
 
 `status`: **GREEN_PENDING_DIRECTOR**  
 `candidate`: **FREEZE_CANDIDATE_PENDING_DIRECTOR**  
 `H1`: **AWAITING_DIRECTOR_DISPOSITION**
 
-Source HR3 SHA-256: `2c172ddd986d0c2c6e970803bffae4313e59c71dd571b36635e132ddcf7c9454`  
+Source HR3R SHA-256: `072d936e4bef77c097b464f6aaf414feb630cafaba6a533f9dfbb9e5c4256018`  
 Authority contract SHA-256: `e15885cc11777ad49f2b0b5ad2778facb64ee6f932b65bf36742a65afe451ba9`  
 Frozen NC contract SHA-256: `8b150937b002d7a054e098182d6453711c477f8eaeb1865c1663541dc135f70d`  
 Frozen S4A: `95d09a2429bf93e4060d65190e5ec369ccc88828fe8ae0ce07a7bfcdb7bf08b8`
 
-P1–P16 are **16/16 PASS** and C1–C25 are **25/25 PASS**. No C26 or later gate exists. The canonical current record is `evidence/s5-authority-report.json`, SHA-256 `310eec49774abb061d2adc65548a6a582cfc6c1ce57ac0957301197e9269f59e`. Detailed implementation evidence is in `MT1_S5HR3R_CLOSURE.md`.
+P1–P16 are **16/16 PASS** and C1–C25 are **25/25 PASS**. No C gate or frozen control was added. The canonical current record is `evidence/s5-authority-report.json`, SHA-256 `d1018ae367e2bf22281addec845a0d0a2469cbebcfa5f49aef1dc50dd0c2784a`. Detailed implementation evidence is in `MT1_S5HR3R1_CLOSURE.md`.
 
-The nominal certificate contains 394 unique registered rows, 365 live physical rows, 95 lifetime-relevant rows, 2,002 live-prism samples, and 2,981,632 pair evaluations. Exact topology is 98/98. Moving↔moving contributes 56,112 evaluations over 28 unique ID pairs. The immutable intentional-contact authority instantiates 223 exact registration-ID relations across nine rule classes with no missing rows.
+Authority identity preflight now dominates every S5 proof projection. After uniqueness passes, frozen semantic names bind once to immutable opaque registration IDs. All later solid selection, mutation, path history, support/load traversal, material and supersession scope, collision/contact authorization, and proof pairing use registration IDs. Duplicate names retain both conflicting IDs and stop at zero motion samples and zero evaluated proof pairs, before support, load-path, or passive-contact projection.
 
-Proof identity is now bound once after registration and unique-name validation. All subsequent topology, load-path, required-row, pair, and contact proof selection uses registration IDs. Duplicate aliasing stops before required-topology projection. First-relevance evidence retains the frozen analytic phase. The legacy broad prefix contact waiver is absent; the exact ID table is the sole S5 contact authority.
+The nominal certificate contains 394 unique registered rows, 365 live physical rows, 95 lifetime-relevant rows, 2,002 live-prism samples, and 2,981,632 identity-based pair evaluations. Exact topology is 98/98. The support graph has 70 registration-ID edges and no missing continuity. Moving↔moving contributes 56,112 evaluations over 28 unique ID pairs. The immutable intentional-contact authority instantiates 223 exact registration-ID relations across nine rule classes with no missing rows.
 
-The wrong-sector conformance regression moves registered `S5_SPIGOT_STBD` onto the PORT receiver. Because that ID pair is not declared, ordinary collision handling records the physical hit and makes C18, certificate, report, evaluator, raw apply, and getter readiness false. The four nominal same-sector declared relations remain valid.
+No S5 proof-semantic `getMeshByName()` remains. Supersession and S5 material/class decisions use registration-ID maps. Names remain only as human-readable diagnostics and as the one-time frozen semantic-name→registration-ID binding performed after global name uniqueness succeeds.
 
-Forward/reverse metrics remain unchanged from HR3: endpoint extension 0.024540 m; worst retained insertion 0.006540007 m side and 0.018540070 m top; shoulder-clear lead 0.005053427 m side and 0.001738427 m top. Readiness is false through machineT 0.99982, first stable true at 0.99983, and true through endpoint.
+V2 remains CLOSED: the immutable exact-pair intentional-contact table is the sole S5 contact authority. V3 remains CLOSED: the wrong-sector registered spigot/receiver pair is not authorized and fails ordinary collision handling. `NC-MIDPATH-TRANSIENT-ROW` retains its first-relevant row and taxonomy by ID; `NC-DUPLICATE-AUTHORITY-NAME` fails before proof projection; `NC-DUPLICATE-MOVING-ROW` is detected by the moving↔moving pair proof.
 
-The strict atomic ledger is **28/28 PASS**, zero unexpected failures, and 28/28 independent raw/evaluator/getter/certificate restorations. Its SHA-256 is `a00d18a8353b2b24639bfe57f2efe9b7cb5304b524ae0ad1e0a71dba7232a0a9`.
+Nominal forward/reverse mechanics remain unchanged: endpoint extension 0.024540 m; worst retained insertion 0.006540007 m side and 0.018540070 m top; shoulder-clear lead 0.005053427 m side and 0.001738427 m top. Readiness is false through machineT 0.99982, first stable true at 0.99983, and true through endpoint.
 
-Supported verification: locked install PASS; build PASS; focused HR3R 7/7 PASS in 9.3 minutes; bounded S4 6/6 PASS in 6.6 minutes; `npm test` 95/95 PASS in 31.4 minutes; authority writer 1/1 PASS in 3.9 minutes; atomic evidence writer 1/1 PASS in 47.0 minutes. Default tests did not execute evidence writers, and all historical evidence outside the two authorized current S5 records remained byte-identical.
+The strict atomic ledger is **28/28 PASS**, with zero unexpected failures and 28/28 independent raw/evaluator/getter/certificate restorations. Its SHA-256 is `076385f5cc1143f96143a3840799e0ae0a521a80bcd09d7f7afaa956a0fb4e2b`.
 
-H1 remains director-only. Nominal S5 geometry, B′, frozen contracts, frozen S4A/DP1 authority, the 28-control battery, and historical evidence are unchanged. No S6 or propulsion-performance claim is made.
+Supported verification: build PASS; focused HR3R1 7/7 PASS in 9.9 minutes; bounded S4 6/6 PASS in 5.0 minutes; final exact-state `npm test` 95/95 PASS in 28.5 minutes; authority writer 1/1 PASS in 3.6 minutes; atomic evidence writer 1/1 PASS in 43.4 minutes; strict ledger validator 1/1 PASS. Default tests did not execute evidence writers, and all historical evidence outside the two authorized current S5 records remained byte-identical.
+
+H1 remains director-only. Nominal S5 geometry, B′, frozen contracts, frozen S4A/DP1 authority, the 28-control battery, C1–C25, and historical evidence are unchanged. No S6 or propulsion-performance claim is made.
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=test-results' '--exclude=*.zip' '--exclude=*.diff' '--exclude=*.Zone.Identifier' MT1-S5HR3R/README.md MT1-S5HR3R1/README.md
--- MT1-S5HR3R/README.md	2026-08-25 14:45:01.000000000 -0500
+++ MT1-S5HR3R1/README.md	2026-08-26 19:13:13.377968595 -0500
@@ -1,4 +1,4 @@
-# MT1-S5HR3R — Frozen-Contract Compliance Closure
+# MT1-S5HR3R1 — Final V1 Proof-Identity Migration
 
 Mechanical Truth third canonical gate. Certified S1C rear and S2A front authorities are frozen. S3 adds one reversible `machineT` that orchestrates both laterals and the axial drive as a single SPREAD↔DRIVE event without changing local geometry.
 
@@ -8,7 +8,7 @@
 
 This is an interference-set and authority proof, not a vehicle.
 
-Certified mechanism baseline: **MT1-S4A** (frozen). Current slice: **MT1-S5HR3R**, the bounded proof-identity, first-relevance phase, and exact-contact compliance closure for HR3. Nominal geometry and B′ are unchanged. Automated S5HR3R gates target GREEN_PENDING_DIRECTOR; H1 remains AWAITING_DIRECTOR_DISPOSITION.
+Certified mechanism baseline: **MT1-S4A** (frozen). Current slice: **MT1-S5HR3R1**, the final bounded V1 proof-identity migration. Registration ID is the sole post-preflight physical proof identity; nominal geometry and B′ are unchanged. Automated S5HR3R1 gates target GREEN_PENDING_DIRECTOR; H1 remains AWAITING_DIRECTOR_DISPOSITION.
 
 **MT1-VB1** is a separate informative vehicle-balance study (`VB1_CANDIDATE_PENDING_DIRECTOR_ASSUMPTION_REVIEW`). It is not S5. No cockpit/intake/propulsion construction.
 
@@ -83,7 +83,7 @@
 window.__MT1.getAuthorityMode()
 window.__MT1.runFrontClearance()   // frozen S2A
 window.__MT1.runClearance()        // frozen S1C
-window.__MT1.runS5Authority()      // current S5HR3R
+window.__MT1.runS5Authority()      // current S5HR3R1
 window.__MT1.getDriveThrustReady()
 ```
 
@@ -109,6 +109,6 @@
 VB1: `MT1_VB1_SCOPE_AND_EPISTEMICS.md`, `MT1_VB1_MASS_ASSUMPTIONS.md`, `MT1_VB1_GEOMETRY_FACTS.md`, `MT1_VB1_RESULTS.md`, `evidence/vb1-*.json`  
 VB1-AR1 (F5 location addendum only): `MT1_VB1_F5_LOCATION_ADDENDUM.md`, `evidence/vb1-f5-*`  
 DP1 / DP1A (bounded drive/propulsion architecture study, not S5): `MT1_DP1_*.md`, `evidence/dp1-*`  
-S5 through S5HR3R (physical propulsion handover and bounded local authority correctives, not S6): `MT1_S5_*.md`, `MT1_S5A_CLOSURE.md` … `MT1_S5HR3R_CLOSURE.md`, `evidence/s5-*`, `evidence/s5h-*`
+S5 through S5HR3R1 (physical propulsion handover and bounded local authority correctives, not S6): `MT1_S5_*.md`, `MT1_S5A_CLOSURE.md` … `MT1_S5HR3R1_CLOSURE.md`, `evidence/s5-*`, `evidence/s5h-*`
 
 Preserve prior freeze archives unchanged. Do not begin S6, cockpit/intake architecture, engine-detail work, aero/CFD, or propulsion-performance work.
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=test-results' '--exclude=*.zip' '--exclude=*.diff' '--exclude=*.Zone.Identifier' MT1-S5HR3R/evidence/s5-authority-report.json MT1-S5HR3R1/evidence/s5-authority-report.json
--- MT1-S5HR3R/evidence/s5-authority-report.json	2026-08-25 15:38:52.000000000 -0500
+++ MT1-S5HR3R1/evidence/s5-authority-report.json	2026-08-26 17:26:41.144869725 -0500
@@ -1,5 +1,5 @@
 {
-  "freezeId": "MT1-S5HR3R",
+  "freezeId": "MT1-S5HR3R1",
   "sourceS4aSha256": "95d09a2429bf93e4060d65190e5ec369ccc88828fe8ae0ce07a7bfcdb7bf08b8",
   "sourceDp1arSha256": "3d63b1d08566fbc095713685d39570aae9fc5f454272eddb273e45e24c24fb71",
   "status": "GREEN_PENDING_DIRECTOR",
@@ -560,7 +560,7 @@
   "census": {
     "LIVE_MATERIAL_PHYSICAL": 365,
     "LEGACY_SUPERSEDED_REFERENCE": 2,
-    "DEBUG_NONPHYSICAL": 27,
+    "DEBUG_NONPHYSICAL": 19,
     "REGISTERED_AUTHORITY_ROWS": 394,
     "retainedS4a": 5,
     "newS5": 154,
@@ -849,351 +849,491 @@
       "missing": [],
       "edges": [
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00007Y",
+          "toRegistrationId": "AUTHORITY_ROW_00008C",
           "from": "S5_LOCK_RAIL_A_PORT_MOUTH",
           "to": "S5_LOCK_TRACK_BACK_PORT",
           "sep": -1.6403198220202309e-7
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00007Z",
+          "toRegistrationId": "AUTHORITY_ROW_00008C",
           "from": "S5_LOCK_RAIL_A_PORT_LEAD",
           "to": "S5_LOCK_TRACK_BACK_PORT",
           "sep": -0.003999951362609864
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_000080",
+          "toRegistrationId": "AUTHORITY_ROW_00008C",
           "from": "S5_LOCK_RAIL_A_PORT_TRANSITION",
           "to": "S5_LOCK_TRACK_BACK_PORT",
           "sep": -0.003999951362609864
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_000081",
+          "toRegistrationId": "AUTHORITY_ROW_00008C",
           "from": "S5_LOCK_RAIL_A_PORT_CAM_ENTRY",
           "to": "S5_LOCK_TRACK_BACK_PORT",
           "sep": -0.003999951362609864
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_000082",
+          "toRegistrationId": "AUTHORITY_ROW_00008C",
           "from": "S5_LOCK_RAIL_A_PORT_CAM",
           "to": "S5_LOCK_TRACK_BACK_PORT",
           "sep": -0.003999951362609864
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_000083",
+          "toRegistrationId": "AUTHORITY_ROW_00008C",
           "from": "S5_LOCK_RAIL_A_PORT_CAM_EXIT",
           "to": "S5_LOCK_TRACK_BACK_PORT",
           "sep": -0.003999951362609864
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_000084",
+          "toRegistrationId": "AUTHORITY_ROW_00008C",
           "from": "S5_LOCK_RAIL_A_PORT_CAPTURE",
           "to": "S5_LOCK_TRACK_BACK_PORT",
           "sep": -0.003999951362609864
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_000085",
+          "toRegistrationId": "AUTHORITY_ROW_00008C",
           "from": "S5_LOCK_RAIL_B_PORT_MOUTH",
           "to": "S5_LOCK_TRACK_BACK_PORT",
           "sep": -1.6403198220202309e-7
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_000086",
+          "toRegistrationId": "AUTHORITY_ROW_00008C",
           "from": "S5_LOCK_RAIL_B_PORT_LEAD",
           "to": "S5_LOCK_TRACK_BACK_PORT",
           "sep": -0.003999951362609864
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_000087",
+          "toRegistrationId": "AUTHORITY_ROW_00008C",
           "from": "S5_LOCK_RAIL_B_PORT_TRANSITION",
           "to": "S5_LOCK_TRACK_BACK_PORT",
           "sep": -0.003999951362609864
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_000088",
+          "toRegistrationId": "AUTHORITY_ROW_00008C",
           "from": "S5_LOCK_RAIL_B_PORT_CAM_ENTRY",
           "to": "S5_LOCK_TRACK_BACK_PORT",
           "sep": -0.003999951362609864
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_000089",
+          "toRegistrationId": "AUTHORITY_ROW_00008C",
           "from": "S5_LOCK_RAIL_B_PORT_CAM",
           "to": "S5_LOCK_TRACK_BACK_PORT",
           "sep": -0.003999951362609864
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00008A",
+          "toRegistrationId": "AUTHORITY_ROW_00008C",
           "from": "S5_LOCK_RAIL_B_PORT_CAM_EXIT",
           "to": "S5_LOCK_TRACK_BACK_PORT",
           "sep": -0.003999951362609864
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00008B",
+          "toRegistrationId": "AUTHORITY_ROW_00008C",
           "from": "S5_LOCK_RAIL_B_PORT_CAPTURE",
           "to": "S5_LOCK_TRACK_BACK_PORT",
           "sep": -0.003999951362609864
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00008C",
+          "toRegistrationId": "AUTHORITY_ROW_00008K",
           "from": "S5_LOCK_TRACK_BACK_PORT",
           "to": "S5_LOCK_POST_PORT",
           "sep": -0.00800004148483277
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00008P",
+          "toRegistrationId": "AUTHORITY_ROW_000093",
           "from": "S5_LOCK_RAIL_A_STARBOARD_MOUTH",
           "to": "S5_LOCK_TRACK_BACK_STARBOARD",
           "sep": -1.6403198220202309e-7
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00008Q",
+          "toRegistrationId": "AUTHORITY_ROW_000093",
           "from": "S5_LOCK_RAIL_A_STARBOARD_LEAD",
           "to": "S5_LOCK_TRACK_BACK_STARBOARD",
           "sep": -0.003999951362609864
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00008R",
+          "toRegistrationId": "AUTHORITY_ROW_000093",
           "from": "S5_LOCK_RAIL_A_STARBOARD_TRANSITION",
           "to": "S5_LOCK_TRACK_BACK_STARBOARD",
           "sep": -0.003999951362609864
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00008S",
+          "toRegistrationId": "AUTHORITY_ROW_000093",
           "from": "S5_LOCK_RAIL_A_STARBOARD_CAM_ENTRY",
           "to": "S5_LOCK_TRACK_BACK_STARBOARD",
           "sep": -0.003999951362609864
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00008T",
+          "toRegistrationId": "AUTHORITY_ROW_000093",
           "from": "S5_LOCK_RAIL_A_STARBOARD_CAM",
           "to": "S5_LOCK_TRACK_BACK_STARBOARD",
           "sep": -0.003999951362609864
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00008U",
+          "toRegistrationId": "AUTHORITY_ROW_000093",
           "from": "S5_LOCK_RAIL_A_STARBOARD_CAM_EXIT",
           "to": "S5_LOCK_TRACK_BACK_STARBOARD",
           "sep": -0.003999951362609864
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00008V",
+          "toRegistrationId": "AUTHORITY_ROW_000093",
           "from": "S5_LOCK_RAIL_A_STARBOARD_CAPTURE",
           "to": "S5_LOCK_TRACK_BACK_STARBOARD",
           "sep": -0.003999951362609864
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00008W",
+          "toRegistrationId": "AUTHORITY_ROW_000093",
           "from": "S5_LOCK_RAIL_B_STARBOARD_MOUTH",
           "to": "S5_LOCK_TRACK_BACK_STARBOARD",
           "sep": -1.6403198220202309e-7
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00008X",
+          "toRegistrationId": "AUTHORITY_ROW_000093",
           "from": "S5_LOCK_RAIL_B_STARBOARD_LEAD",
           "to": "S5_LOCK_TRACK_BACK_STARBOARD",
           "sep": -0.003999951362609864
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00008Y",
+          "toRegistrationId": "AUTHORITY_ROW_000093",
           "from": "S5_LOCK_RAIL_B_STARBOARD_TRANSITION",
           "to": "S5_LOCK_TRACK_BACK_STARBOARD",
           "sep": -0.003999951362609864
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00008Z",
+          "toRegistrationId": "AUTHORITY_ROW_000093",
           "from": "S5_LOCK_RAIL_B_STARBOARD_CAM_ENTRY",
           "to": "S5_LOCK_TRACK_BACK_STARBOARD",
           "sep": -0.003999951362609864
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_000090",
+          "toRegistrationId": "AUTHORITY_ROW_000093",
           "from": "S5_LOCK_RAIL_B_STARBOARD_CAM",
           "to": "S5_LOCK_TRACK_BACK_STARBOARD",
           "sep": -0.003999951362609864
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_000091",
+          "toRegistrationId": "AUTHORITY_ROW_000093",
           "from": "S5_LOCK_RAIL_B_STARBOARD_CAM_EXIT",
           "to": "S5_LOCK_TRACK_BACK_STARBOARD",
           "sep": -0.003999951362609864
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_000092",
+          "toRegistrationId": "AUTHORITY_ROW_000093",
           "from": "S5_LOCK_RAIL_B_STARBOARD_CAPTURE",
           "to": "S5_LOCK_TRACK_BACK_STARBOARD",
           "sep": -0.003999951362609864
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_000093",
+          "toRegistrationId": "AUTHORITY_ROW_000096",
           "from": "S5_LOCK_TRACK_BACK_STARBOARD",
           "to": "S5_LOCK_POST_STARBOARD",
           "sep": -0.00800004148483277
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00009B",
+          "toRegistrationId": "AUTHORITY_ROW_00009R",
           "from": "S5_LOCK_RAIL_A_TOP_PORT_MOUTH",
           "to": "S5_LOCK_TRACK_BACK_MOUTH_TOP_PORT",
           "sep": -2.1457672132185746e-9
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00009C",
+          "toRegistrationId": "AUTHORITY_ROW_00009R",
           "from": "S5_LOCK_RAIL_A_TOP_PORT_LEAD",
           "to": "S5_LOCK_TRACK_BACK_MOUTH_TOP_PORT",
           "sep": -2.1457672132185746e-9
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00009D",
+          "toRegistrationId": "AUTHORITY_ROW_00009R",
           "from": "S5_LOCK_RAIL_A_TOP_PORT_TRANSITION",
           "to": "S5_LOCK_TRACK_BACK_MOUTH_TOP_PORT",
           "sep": -2.1457672132185746e-9
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00009E",
+          "toRegistrationId": "AUTHORITY_ROW_00009R",
           "from": "S5_LOCK_RAIL_A_TOP_PORT_CAM_ENTRY",
           "to": "S5_LOCK_TRACK_BACK_MOUTH_TOP_PORT",
           "sep": -2.1457672132185746e-9
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00009F",
+          "toRegistrationId": "AUTHORITY_ROW_00009R",
           "from": "S5_LOCK_RAIL_A_TOP_PORT_CAM",
           "to": "S5_LOCK_TRACK_BACK_MOUTH_TOP_PORT",
           "sep": -2.1457672132185746e-9
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00009G",
+          "toRegistrationId": "AUTHORITY_ROW_00009R",
           "from": "S5_LOCK_RAIL_A_TOP_PORT_CAM_EXIT",
           "to": "S5_LOCK_TRACK_BACK_MOUTH_TOP_PORT",
           "sep": -2.1457672132185746e-9
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00009H",
+          "toRegistrationId": "AUTHORITY_ROW_00009P",
           "from": "S5_LOCK_RAIL_A_TOP_PORT_CAPTURE",
           "to": "S5_LOCK_TRACK_BACK_AFT_TOP_PORT",
           "sep": -0.0009999771118163019
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00009I",
+          "toRegistrationId": "AUTHORITY_ROW_00009R",
           "from": "S5_LOCK_RAIL_B_TOP_PORT_MOUTH",
           "to": "S5_LOCK_TRACK_BACK_MOUTH_TOP_PORT",
           "sep": -2.1457672132185746e-9
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00009J",
+          "toRegistrationId": "AUTHORITY_ROW_00009R",
           "from": "S5_LOCK_RAIL_B_TOP_PORT_LEAD",
           "to": "S5_LOCK_TRACK_BACK_MOUTH_TOP_PORT",
           "sep": -2.1457672132185746e-9
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00009K",
+          "toRegistrationId": "AUTHORITY_ROW_00009R",
           "from": "S5_LOCK_RAIL_B_TOP_PORT_TRANSITION",
           "to": "S5_LOCK_TRACK_BACK_MOUTH_TOP_PORT",
           "sep": -2.1457672132185746e-9
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00009L",
+          "toRegistrationId": "AUTHORITY_ROW_00009R",
           "from": "S5_LOCK_RAIL_B_TOP_PORT_CAM_ENTRY",
           "to": "S5_LOCK_TRACK_BACK_MOUTH_TOP_PORT",
           "sep": -2.1457672132185746e-9
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00009M",
+          "toRegistrationId": "AUTHORITY_ROW_00009R",
           "from": "S5_LOCK_RAIL_B_TOP_PORT_CAM",
           "to": "S5_LOCK_TRACK_BACK_MOUTH_TOP_PORT",
           "sep": -2.1457672132185746e-9
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00009N",
+          "toRegistrationId": "AUTHORITY_ROW_00009R",
           "from": "S5_LOCK_RAIL_B_TOP_PORT_CAM_EXIT",
           "to": "S5_LOCK_TRACK_BACK_MOUTH_TOP_PORT",
           "sep": -2.1457672132185746e-9
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00009O",
+          "toRegistrationId": "AUTHORITY_ROW_00009P",
           "from": "S5_LOCK_RAIL_B_TOP_PORT_CAPTURE",
           "to": "S5_LOCK_TRACK_BACK_AFT_TOP_PORT",
           "sep": -0.0010001068115237204
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00009P",
+          "toRegistrationId": "AUTHORITY_ROW_00009Q",
           "from": "S5_LOCK_TRACK_BACK_AFT_TOP_PORT",
           "to": "S5_LOCK_TRACK_BACK_TOP_PORT",
           "sep": -0.002000225067138671
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00009Q",
+          "toRegistrationId": "AUTHORITY_ROW_00009S",
           "from": "S5_LOCK_TRACK_BACK_TOP_PORT",
           "to": "S5_LOCK_TRACK_BACK_MOUTH_BRIDGE_TOP_PORT",
           "sep": -0.01100007724761963
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00009R",
+          "toRegistrationId": "AUTHORITY_ROW_00009S",
           "from": "S5_LOCK_TRACK_BACK_MOUTH_TOP_PORT",
           "to": "S5_LOCK_TRACK_BACK_MOUTH_BRIDGE_TOP_PORT",
           "sep": -0.0005000028610229468
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00009P",
+          "toRegistrationId": "AUTHORITY_ROW_00009V",
           "from": "S5_LOCK_TRACK_BACK_AFT_TOP_PORT",
           "to": "S5_LOCK_POST_TOP_PORT",
           "sep": -0.030000030517578123
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00009Q",
+          "toRegistrationId": "AUTHORITY_ROW_00009V",
           "from": "S5_LOCK_TRACK_BACK_TOP_PORT",
           "to": "S5_LOCK_POST_TOP_PORT",
           "sep": -0.009000040054321288
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_00009S",
+          "toRegistrationId": "AUTHORITY_ROW_00009V",
           "from": "S5_LOCK_TRACK_BACK_MOUTH_BRIDGE_TOP_PORT",
           "to": "S5_LOCK_POST_TOP_PORT",
           "sep": -0.009999962806701658
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_0000A0",
+          "toRegistrationId": "AUTHORITY_ROW_0000AG",
           "from": "S5_LOCK_RAIL_A_TOP_STARBOARD_MOUTH",
           "to": "S5_LOCK_TRACK_BACK_MOUTH_TOP_STARBOARD",
           "sep": -2.1457672132185746e-9
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_0000A1",
+          "toRegistrationId": "AUTHORITY_ROW_0000AG",
           "from": "S5_LOCK_RAIL_A_TOP_STARBOARD_LEAD",
           "to": "S5_LOCK_TRACK_BACK_MOUTH_TOP_STARBOARD",
           "sep": -2.1457672132185746e-9
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_0000A2",
+          "toRegistrationId": "AUTHORITY_ROW_0000AG",
           "from": "S5_LOCK_RAIL_A_TOP_STARBOARD_TRANSITION",
           "to": "S5_LOCK_TRACK_BACK_MOUTH_TOP_STARBOARD",
           "sep": -2.1457672132185746e-9
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_0000A3",
+          "toRegistrationId": "AUTHORITY_ROW_0000AG",
           "from": "S5_LOCK_RAIL_A_TOP_STARBOARD_CAM_ENTRY",
           "to": "S5_LOCK_TRACK_BACK_MOUTH_TOP_STARBOARD",
           "sep": -2.1457672132185746e-9
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_0000A4",
+          "toRegistrationId": "AUTHORITY_ROW_0000AG",
           "from": "S5_LOCK_RAIL_A_TOP_STARBOARD_CAM",
           "to": "S5_LOCK_TRACK_BACK_MOUTH_TOP_STARBOARD",
           "sep": -2.1457672132185746e-9
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_0000A5",
+          "toRegistrationId": "AUTHORITY_ROW_0000AG",
           "from": "S5_LOCK_RAIL_A_TOP_STARBOARD_CAM_EXIT",
           "to": "S5_LOCK_TRACK_BACK_MOUTH_TOP_STARBOARD",
           "sep": -2.1457672132185746e-9
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_0000A6",
+          "toRegistrationId": "AUTHORITY_ROW_0000AE",
           "from": "S5_LOCK_RAIL_A_TOP_STARBOARD_CAPTURE",
           "to": "S5_LOCK_TRACK_BACK_AFT_TOP_STARBOARD",
           "sep": -0.0009999771118163019
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_0000A7",
+          "toRegistrationId": "AUTHORITY_ROW_0000AG",
           "from": "S5_LOCK_RAIL_B_TOP_STARBOARD_MOUTH",
           "to": "S5_LOCK_TRACK_BACK_MOUTH_TOP_STARBOARD",
           "sep": -2.1457672132185746e-9
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_0000A8",
+          "toRegistrationId": "AUTHORITY_ROW_0000AG",
           "from": "S5_LOCK_RAIL_B_TOP_STARBOARD_LEAD",
           "to": "S5_LOCK_TRACK_BACK_MOUTH_TOP_STARBOARD",
           "sep": -2.1457672132185746e-9
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_0000A9",
+          "toRegistrationId": "AUTHORITY_ROW_0000AG",
           "from": "S5_LOCK_RAIL_B_TOP_STARBOARD_TRANSITION",
           "to": "S5_LOCK_TRACK_BACK_MOUTH_TOP_STARBOARD",
           "sep": -2.1457672132185746e-9
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_0000AA",
+          "toRegistrationId": "AUTHORITY_ROW_0000AG",
           "from": "S5_LOCK_RAIL_B_TOP_STARBOARD_CAM_ENTRY",
           "to": "S5_LOCK_TRACK_BACK_MOUTH_TOP_STARBOARD",
           "sep": -2.1457672132185746e-9
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_0000AB",
+          "toRegistrationId": "AUTHORITY_ROW_0000AG",
           "from": "S5_LOCK_RAIL_B_TOP_STARBOARD_CAM",
           "to": "S5_LOCK_TRACK_BACK_MOUTH_TOP_STARBOARD",
           "sep": -2.1457672132185746e-9
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_0000AC",
+          "toRegistrationId": "AUTHORITY_ROW_0000AG",
           "from": "S5_LOCK_RAIL_B_TOP_STARBOARD_CAM_EXIT",
           "to": "S5_LOCK_TRACK_BACK_MOUTH_TOP_STARBOARD",
           "sep": -2.1457672132185746e-9
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_0000AD",
+          "toRegistrationId": "AUTHORITY_ROW_0000AE",
           "from": "S5_LOCK_RAIL_B_TOP_STARBOARD_CAPTURE",
           "to": "S5_LOCK_TRACK_BACK_AFT_TOP_STARBOARD",
           "sep": -0.0010001068115237204
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_0000AE",
+          "toRegistrationId": "AUTHORITY_ROW_0000AF",
           "from": "S5_LOCK_TRACK_BACK_AFT_TOP_STARBOARD",
           "to": "S5_LOCK_TRACK_BACK_TOP_STARBOARD",
           "sep": -0.002000225067138671
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_0000AF",
+          "toRegistrationId": "AUTHORITY_ROW_0000AH",
           "from": "S5_LOCK_TRACK_BACK_TOP_STARBOARD",
           "to": "S5_LOCK_TRACK_BACK_MOUTH_BRIDGE_TOP_STARBOARD",
           "sep": -0.01100007724761963
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_0000AG",
+          "toRegistrationId": "AUTHORITY_ROW_0000AH",
           "from": "S5_LOCK_TRACK_BACK_MOUTH_TOP_STARBOARD",
           "to": "S5_LOCK_TRACK_BACK_MOUTH_BRIDGE_TOP_STARBOARD",
           "sep": -0.0005000028610229468
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_0000AE",
+          "toRegistrationId": "AUTHORITY_ROW_0000AK",
           "from": "S5_LOCK_TRACK_BACK_AFT_TOP_STARBOARD",
           "to": "S5_LOCK_POST_TOP_STARBOARD",
           "sep": -0.030000030517578123
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_0000AF",
+          "toRegistrationId": "AUTHORITY_ROW_0000AK",
           "from": "S5_LOCK_TRACK_BACK_TOP_STARBOARD",
           "to": "S5_LOCK_POST_TOP_STARBOARD",
           "sep": -0.009000040054321288
         },
         {
+          "fromRegistrationId": "AUTHORITY_ROW_0000AH",
+          "toRegistrationId": "AUTHORITY_ROW_0000AK",
           "from": "S5_LOCK_TRACK_BACK_MOUTH_BRIDGE_TOP_STARBOARD",
           "to": "S5_LOCK_POST_TOP_STARBOARD",
           "sep": -0.009999962806701658
@@ -1253,6 +1393,7 @@
       "samples": 2002,
       "pairsEvaluated": 2981632,
       "firstHit": "",
+      "firstHitRegistrationIds": [],
       "minPinBacking": 0.0023049845695495286,
       "minShoeBacking": 0.0029999985694885156,
       "minShoeRail": 1.6471266785073014e-8,
@@ -2643,6 +2784,10 @@
           "S5_LOCK_CAM_SHOE_TOP_PORT",
           "S5_LOCK_CAM_SHOE_TOP_STARBOARD"
         ],
+        "pathAddedRegistrationIds": [
+          "AUTHORITY_ROW_00006Z",
+          "AUTHORITY_ROW_000070"
+        ],
         "approvedSurrounding": [
           "S5_CAN_PORT",
           "S5_CAN_PORT_ABOVE",
@@ -2658,10 +2803,27 @@
           "S5_CAN_TOP_PORT_OUT",
           "S5_CAN_TOP_STBD_OUT"
         ],
+        "approvedSurroundingRegistrationIds": [
+          "AUTHORITY_ROW_000066",
+          "AUTHORITY_ROW_000067",
+          "AUTHORITY_ROW_000068",
+          "AUTHORITY_ROW_000069",
+          "AUTHORITY_ROW_00006B",
+          "AUTHORITY_ROW_00006C",
+          "AUTHORITY_ROW_00006D",
+          "AUTHORITY_ROW_00006E",
+          "AUTHORITY_ROW_00006F",
+          "AUTHORITY_ROW_00006G",
+          "AUTHORITY_ROW_00006H",
+          "AUTHORITY_ROW_00006I",
+          "AUTHORITY_ROW_00006J"
+        ],
         "unclassified": [],
+        "unclassifiedRegistrationIds": [],
         "invalidClassification": [],
         "conflictingClassification": [],
         "missingRequired": [],
+        "missingRequiredRegistrationIds": [],
         "collisionScope": [
           "S5_CAN_PORT",
           "S5_CAN_PORT_ABOVE",
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=test-results' '--exclude=*.zip' '--exclude=*.diff' '--exclude=*.Zone.Identifier' MT1-S5HR3R/evidence/s5-negative-control-results.json MT1-S5HR3R1/evidence/s5-negative-control-results.json
--- MT1-S5HR3R/evidence/s5-negative-control-results.json	2026-08-25 15:34:36.000000000 -0500
+++ MT1-S5HR3R1/evidence/s5-negative-control-results.json	2026-08-26 19:00:37.198582401 -0500
@@ -1,5 +1,5 @@
 {
-  "schema": "MT1-S5HR3R-negative-control-ledger-v1",
+  "schema": "MT1-S5HR3R1-negative-control-ledger-v1",
   "complete": true,
   "controls": [
     {
@@ -1430,13 +1430,27 @@
       "mutationApplied": true,
       "expectedFailingGates": [],
       "allowedCascadeContract": [
+        "S5-P3",
+        "S5-P4",
+        "S5-P5",
         "S5-P7",
+        "S5-P8",
+        "S5-P9",
+        "S5-P10",
+        "S5-P11",
         "S5-P12",
         "S5-P14",
+        "C2",
+        "C3",
+        "C4",
         "C6",
+        "C7",
         "C8",
+        "C9",
+        "C13",
         "C14",
         "C15",
+        "C16",
         "C17",
         "C19",
         "C21",
@@ -1445,12 +1459,26 @@
         "C25"
       ],
       "allowedCascadeFailures": [
+        "S5-P3",
+        "S5-P4",
+        "S5-P5",
         "S5-P7",
+        "S5-P8",
+        "S5-P9",
+        "S5-P10",
+        "S5-P11",
         "S5-P14",
+        "C2",
+        "C3",
+        "C4",
         "C6",
+        "C7",
         "C8",
+        "C9",
+        "C13",
         "C14",
         "C15",
+        "C16",
         "C17",
         "C19",
         "C21",
@@ -1459,17 +1487,31 @@
         "C25"
       ],
       "actualFailedGates": [
+        "S5-P3",
+        "S5-P4",
+        "S5-P5",
         "S5-P7",
+        "S5-P8",
+        "S5-P9",
+        "S5-P10",
+        "S5-P11",
         "S5-P14"
       ],
       "expectedFailedClosures": [
         "C18"
       ],
       "actualFailedClosures": [
+        "C2",
+        "C3",
+        "C4",
         "C6",
+        "C7",
         "C8",
+        "C9",
+        "C13",
         "C14",
         "C15",
+        "C16",
         "C17",
         "C18",
         "C19",
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=test-results' '--exclude=*.zip' '--exclude=*.diff' '--exclude=*.Zone.Identifier' MT1-S5HR3R/package.json MT1-S5HR3R1/package.json
--- MT1-S5HR3R/package.json	2026-08-25 14:45:01.000000000 -0500
+++ MT1-S5HR3R1/package.json	2026-08-26 16:11:24.025255874 -0500
@@ -3,7 +3,7 @@
   "private": true,
   "version": "0.1.0",
   "type": "module",
-  "description": "MT1-S5HR3R — frozen-contract compliance closure",
+  "description": "MT1-S5HR3R1 — final V1 proof-identity migration",
   "scripts": {
     "dev": "vite --host 127.0.0.1 --port 5181",
     "build": "tsc --noEmit -p tsconfig.app.json && vite build",
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=test-results' '--exclude=*.zip' '--exclude=*.diff' '--exclude=*.Zone.Identifier' MT1-S5HR3R/src/machine/createS5Machine.ts MT1-S5HR3R1/src/machine/createS5Machine.ts
--- MT1-S5HR3R/src/machine/createS5Machine.ts	2026-08-25 14:23:30.000000000 -0500
+++ MT1-S5HR3R1/src/machine/createS5Machine.ts	2026-08-26 15:55:39.028498434 -0500
@@ -8,7 +8,13 @@
   probeAxialEscape,
 } from "../verify/s5Capture";
 import { createMachine } from "./createMachine";
-import { applyDriveSupersession, physicallySupersedeLegacy, reenableLegacy } from "./s5Authority";
+import {
+  applyDriveSupersession,
+  bindS5AuthoritySemanticsToRegistrationIds,
+  physicallySupersedeLegacy,
+  physicallySupersedeLegacyByRegistrationId,
+  reenableLegacyByRegistrationId,
+} from "./s5Authority";
 import { buildS5Propulsion, type S5Override } from "./s5Propulsion";
 import type { MachineRig } from "./types";
 import { registerS5AuthorityUniverse } from "./s5Identity";
@@ -32,6 +38,8 @@
   rig.solids.push(...s5.solids);
   rig.datums.push(...s5.datums);
   registerS5AuthorityUniverse(rig, rig.solids);
+  bindS5AuthoritySemanticsToRegistrationIds(rig, rig.solids);
+  s5.bindAuthorityRows(rig);
 
   let lastThrust = false;
   const poseLock = (driveT: number): void => {
@@ -61,9 +69,9 @@
     const ov = s5.currentOverride();
     const result = innerPreserve(fn);
     s5.applyOverride(ov);
-    physicallySupersedeLegacy(rig.solids);
-    if (ov.enableLegacyEnvelope) reenableLegacy(rig.solids, "DRIVE_ENVELOPE");
-    if (ov.enableLegacyFace) reenableLegacy(rig.solids, "DRIVE_FACE_AFT");
+    physicallySupersedeLegacyByRegistrationId(rig, rig.solids);
+    if (ov.enableLegacyEnvelope) reenableLegacyByRegistrationId(rig, rig.solids, "DRIVE_ENVELOPE");
+    if (ov.enableLegacyFace) reenableLegacyByRegistrationId(rig, rig.solids, "DRIVE_FACE_AFT");
     poseLock(rig.lastDriveT());
     markPathCertificateStale(rig);
     return result;
@@ -71,10 +79,10 @@
 
   const ext = rig as S5MachineRig;
   ext.applyS5Override = (value) => {
-    physicallySupersedeLegacy(rig.solids);
+    physicallySupersedeLegacyByRegistrationId(rig, rig.solids);
     s5.applyOverride(value ?? {});
-    if (value?.enableLegacyEnvelope) reenableLegacy(rig.solids, "DRIVE_ENVELOPE");
-    if (value?.enableLegacyFace) reenableLegacy(rig.solids, "DRIVE_FACE_AFT");
+    if (value?.enableLegacyEnvelope) reenableLegacyByRegistrationId(rig, rig.solids, "DRIVE_ENVELOPE");
+    if (value?.enableLegacyFace) reenableLegacyByRegistrationId(rig, rig.solids, "DRIVE_FACE_AFT");
     poseLock(rig.lastDriveT());
     markPathCertificateStale(rig);
   };
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=test-results' '--exclude=*.zip' '--exclude=*.diff' '--exclude=*.Zone.Identifier' MT1-S5HR3R/src/machine/s5Authority.ts MT1-S5HR3R1/src/machine/s5Authority.ts
--- MT1-S5HR3R/src/machine/s5Authority.ts	2026-08-18 03:39:02.000000000 -0500
+++ MT1-S5HR3R1/src/machine/s5Authority.ts	2026-08-26 16:11:17.023563205 -0500
@@ -1,4 +1,9 @@
 import type { AuthoritySolid } from "./authority";
+import {
+  auditS5AuthorityIdentity,
+  getS5AuthorityIdentityBinding,
+  registrationIdOf,
+} from "./s5Identity";
 
 export type S5Class =
   | "LIVE_MATERIAL_PHYSICAL"
@@ -10,6 +15,8 @@
 
 const supersededBy = new Map<string, string>();
 const s5Class = new Map<string, S5Class>();
+const supersededByRegistrationId = new Map<string, string>();
+const s5ClassByRegistrationId = new Map<string, S5Class>();
 
 export function markSuperseded(name: string, by: string): void {
   supersededBy.set(name, by);
@@ -20,12 +27,33 @@
   s5Class.set(name, cls);
 }
 
-export function getSupersededBy(name: string): string | undefined {
-  return supersededBy.get(name);
+/**
+ * One-time migration of build-time semantic labels into the frozen registered
+ * identity universe. This may run only after global identity/name preflight.
+ */
+export function bindS5AuthoritySemanticsToRegistrationIds(owner: object, rows: readonly AuthoritySolid[]): void {
+  const audit = auditS5AuthorityIdentity(rows);
+  if (!audit.pass) throw new Error("S5 authority semantics cannot bind before identity preflight passes");
+  const binding = getS5AuthorityIdentityBinding(owner);
+  supersededByRegistrationId.clear();
+  s5ClassByRegistrationId.clear();
+  for (const [semanticName, cls] of s5Class) {
+    const registrationId = binding.registrationIdBySemanticName[semanticName];
+    if (registrationId) s5ClassByRegistrationId.set(registrationId, cls);
+  }
+  for (const [semanticName, replacementSemanticName] of supersededBy) {
+    const registrationId = binding.registrationIdBySemanticName[semanticName];
+    const replacementId = binding.registrationIdBySemanticName[replacementSemanticName];
+    if (registrationId) supersededByRegistrationId.set(registrationId, replacementId ?? replacementSemanticName);
+  }
 }
 
-export function getS5Class(name: string): S5Class | undefined {
-  return s5Class.get(name);
+export function getSupersededByRegistrationId(registrationId: string): string | undefined {
+  return supersededByRegistrationId.get(registrationId);
+}
+
+export function getS5ClassByRegistrationId(registrationId: string): S5Class | undefined {
+  return s5ClassByRegistrationId.get(registrationId);
 }
 
 export function applyDriveSupersession(names: string[]): void {
@@ -48,19 +76,38 @@
   }
 }
 
-export function reenableLegacy(solids: AuthoritySolid[], name: string): void {
-  const s = solids.find((x) => x.name === name);
+/** Post-registration mutation path: selection and material authority are ID-only. */
+export function physicallySupersedeLegacyByRegistrationId(owner: object, solids: AuthoritySolid[]): void {
+  const binding = getS5AuthorityIdentityBinding(owner);
+  const rowsById = new Map(solids.map((row) => [registrationIdOf(row), row]));
+  const replacementId = binding.registrationIdBySemanticName.S5_THRUST_CAN;
+  for (const semanticName of ["DRIVE_ENVELOPE", "DRIVE_FACE_AFT"] as const) {
+    const registrationId = binding.registrationIdBySemanticName[semanticName];
+    const s = rowsById.get(registrationId);
+    if (!s) continue;
+    s.node.setEnabled(false);
+    const mesh = s.node as { visibility?: number; isVisible?: boolean };
+    if (typeof mesh.visibility === "number") mesh.visibility = 0;
+    supersededByRegistrationId.set(registrationId, replacementId);
+    s5ClassByRegistrationId.set(registrationId, "LEGACY_SUPERSEDED_REFERENCE");
+  }
+}
+
+export function reenableLegacyByRegistrationId(owner: object, solids: AuthoritySolid[], semanticName: string): void {
+  const binding = getS5AuthorityIdentityBinding(owner);
+  const registrationId = binding.registrationIdBySemanticName[semanticName];
+  const s = solids.find((row) => registrationIdOf(row) === registrationId);
   if (!s) return;
   s.role = "physical";
   s.node.setEnabled(true);
   const mesh = s.node as { visibility?: number };
   if (typeof mesh.visibility === "number") mesh.visibility = 1;
-  supersededBy.delete(name);
-  s5Class.set(name, "LIVE_MATERIAL_PHYSICAL");
+  supersededByRegistrationId.delete(registrationId);
+  s5ClassByRegistrationId.set(registrationId, "LIVE_MATERIAL_PHYSICAL");
 }
 
-export function isS5MaterialSolid(name: string, role: string, enabled = true): boolean {
-  return role === "physical" && enabled && !supersededBy.has(name);
+export function isS5MaterialRegistration(registrationId: string, role: string, enabled = true): boolean {
+  return role === "physical" && enabled && !supersededByRegistrationId.has(registrationId);
 }
 
 export function meshEnabled(node: { isEnabled?: (checkParents?: boolean) => boolean }): boolean {
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=test-results' '--exclude=*.zip' '--exclude=*.diff' '--exclude=*.Zone.Identifier' MT1-S5HR3R/src/machine/s5Propulsion.ts MT1-S5HR3R1/src/machine/s5Propulsion.ts
--- MT1-S5HR3R/src/machine/s5Propulsion.ts	2026-08-25 14:38:45.000000000 -0500
+++ MT1-S5HR3R1/src/machine/s5Propulsion.ts	2026-08-26 15:56:18.616692178 -0500
@@ -26,6 +26,11 @@
 import type { Materials } from "../scene/materials";
 import { registerBox, type AuthoritySolid } from "./authority";
 import { markS5Class } from "./s5Authority";
+import {
+  auditS5AuthorityIdentity,
+  getS5AuthorityIdentityBinding,
+  registrationIdOf,
+} from "./s5Identity";
 import type { DebugDatum } from "./types";
 
 export interface S5Override {
@@ -106,6 +111,7 @@
   datums: DebugDatum[];
   solids: AuthoritySolid[];
   applyOverride: (opts: S5Override) => void;
+  bindAuthorityRows: (owner: object) => void;
   currentOverride: () => S5Override;
   applyLockPose: (driveT: number, force?: "extend" | "retract" | "gap") => void;
   pins: Record<S5DogId, Mesh>;
@@ -435,8 +441,6 @@
     "MOVING_LOCK_ASSEMBLY",
   );
   duplicateNameMesh.setEnabled(false);
-  const duplicateNameSpec = solids.find((row) => row.node === duplicateNameMesh);
-  if (!duplicateNameSpec) throw new Error("duplicate-name authority fixture registration missing");
   const duplicateMovingMesh = tagAssembly(
     reg(
       solids,
@@ -447,8 +451,6 @@
     "MOVING_LOCK_ASSEMBLY",
   );
   duplicateMovingMesh.setEnabled(false);
-  const duplicateMovingSpec = solids.find((row) => row.node === duplicateMovingMesh);
-  if (!duplicateMovingSpec) throw new Error("duplicate-moving authority fixture registration missing");
 
   reg(
     solids,
@@ -522,6 +524,27 @@
   bottomDog.setEnabled(false);
 
   let live: S5Override = {};
+  let authorityRowsBySemanticName: Readonly<Record<string, AuthoritySolid>> | undefined;
+
+  const bindAuthorityRows = (owner: object): void => {
+    const audit = auditS5AuthorityIdentity(solids);
+    if (!audit.pass) throw new Error("S5 propulsion mutation binding requires successful identity preflight");
+    const binding = getS5AuthorityIdentityBinding(owner);
+    const rowsById = new Map(solids.map((row) => [registrationIdOf(row), row]));
+    authorityRowsBySemanticName = Object.freeze(
+      Object.fromEntries(
+        Object.entries(binding.registrationIdBySemanticName).flatMap(([semanticName, registrationId]) => {
+          const row = rowsById.get(registrationId);
+          return row ? [[semanticName, row] as const] : [];
+        }),
+      ),
+    );
+  };
+
+  const authorityRow = (semanticName: string): AuthoritySolid | undefined => {
+    if (!authorityRowsBySemanticName) throw new Error("S5 propulsion mutation identity binding is absent");
+    return authorityRowsBySemanticName[semanticName];
+  };
 
   const applyLockPose = (driveT: number, force?: "extend" | "retract" | "gap"): void => {
     const transientOn = Boolean(live.midpathTransientRow) && driveT >= 0.98470 && driveT <= 0.98480;
@@ -553,7 +576,7 @@
     const tip = S5P.spigotZFwd - spigotLen;
     const mid = (tip + S5P.spigotZFwd) / 2;
     for (const [index, mesh] of spigotWalls.entries()) {
-      const spec = solids.find((s) => s.name === mesh.name);
+      const spec = registeredSpecForMesh(mesh, solids);
       if (!spec) continue;
       const size = mesh.metadata.size as [number, number, number];
       const rest = spigotWallRest[index]!;
@@ -575,14 +598,14 @@
     for (const id of S5_DOG_IDS) {
       const hideTongue = opts.removeDog === id;
       dogs[id].setEnabled(!hideTongue);
-      const tspec = solids.find((s) => s.name === `S5_SEAT_TONGUE_${id}`);
+      const tspec = authorityRow(`S5_SEAT_TONGUE_${id}`);
       if (tspec) tspec.role = hideTongue ? "diagnostic" : "physical";
       const hideLock = opts.removeLock === id;
       pins[id].setEnabled(!hideLock);
       shoes[id].setEnabled(!hideLock);
-      const pspec = solids.find((s) => s.name === `S5_LOCK_PIN_${id}`);
+      const pspec = authorityRow(`S5_LOCK_PIN_${id}`);
       if (pspec) pspec.role = hideLock ? "diagnostic" : "physical";
-      const fspec = solids.find((s) => s.name === `S5_LOCK_CAM_SHOE_${id}`);
+      const fspec = authorityRow(`S5_LOCK_CAM_SHOE_${id}`);
       if (fspec) fspec.role = hideLock ? "diagnostic" : "physical";
     }
     applyLockPose(1, opts.forceUnlock ? "retract" : opts.lockGap ? "gap" : opts.forceLock ? "extend" : undefined);
@@ -614,28 +637,32 @@
       "S5_LOCK_WEB_TOP_PORT",
       "S5_LOCK_WEB_TOP_STARBOARD",
     ]) {
-      const spec = solids.find((s) => s.name === name);
+      const spec = authorityRow(name);
       if (spec) {
         spec.role = hideFrame ? "diagnostic" : "physical";
         spec.node.setEnabled(!hideFrame);
       }
     }
     for (const id of S5_DOG_IDS) {
-      const web = solids.find((s) => s.name === `S5_LOCK_WEB_${id}`);
+      const web = authorityRow(`S5_LOCK_WEB_${id}`);
       const hideWeb = Boolean(opts.disconnectFrame || opts.railsOnly || opts.floatShoulder === id);
       if (web) {
         web.role = hideWeb ? "diagnostic" : "physical";
         web.node.setEnabled(!hideWeb);
       }
       const layout = s5TrackLayout(id);
-      const rails = solids.filter((s) => s.name.startsWith(`S5_LOCK_RAIL_A_${id}`) || s.name.startsWith(`S5_LOCK_RAIL_B_${id}`));
+      const railNames = ["A", "B"].flatMap((face) =>
+        s5TrackFacePieceLayouts(id, face as "A" | "B").map((piece) => `S5_LOCK_RAIL_${face}_${id}_${piece.kind}`),
+      );
+      const rails = railNames.map(authorityRow).filter(Boolean) as AuthoritySolid[];
       for (const rail of rails) {
         const rest = (rail.node as Mesh).metadata?.rest as [number, number, number] | undefined;
         if (rest) {
           const gap = opts.camGap === id;
           const dx = id === "PORT" ? -0.03 : id === "STARBOARD" ? 0.03 : 0;
           const dy = id.startsWith("TOP") && gap ? 0.03 : 0;
-          const zero = Boolean(opts.zeroMargin) && rail.name.includes("RAIL_B") && (id === "PORT" || id === "STARBOARD");
+          const trackFace = (rail.node as Mesh).metadata?.trackFace as "A" | "B" | undefined;
+          const zero = Boolean(opts.zeroMargin) && trackFace === "B" && (id === "PORT" || id === "STARBOARD");
           const zeroDeltaScale = 3;
           const zeroDelta: [number, number, number] = [
             zeroDeltaScale * (layout.bPosZero[0] - layout.bPos[0]),
@@ -646,37 +673,49 @@
             ? [rest[0] + zeroDelta[0], rest[1] + zeroDelta[1], rest[2] + zeroDelta[2]]
             : rest;
           const normal = ((rail.node as Mesh).metadata?.trackNormal ?? [0, 0, 0]) as [number, number, number];
-          const jam = Boolean(opts.reverseJam && rail.name.includes("RAIL_A")) ? S5_REVERSE_JAM_A_OFFSET : 0;
+          const jam = Boolean(opts.reverseJam && trackFace === "A") ? S5_REVERSE_JAM_A_OFFSET : 0;
           rail.node.position.set(
             zp[0] + (gap ? dx : 0) + normal[0] * jam,
             zp[1] + dy + normal[1] * jam,
             zp[2] + normal[2] * jam,
           );
         }
-        const open = opts.openCam === id && rail.name.includes("RAIL_B");
-        const missingOne = Boolean(opts.missingTrackPiece) && rail.name === "S5_LOCK_RAIL_B_PORT_CAM";
+        const trackFace = (rail.node as Mesh).metadata?.trackFace as "A" | "B" | undefined;
+        const trackPiece = (rail.node as Mesh).metadata?.trackPiece as string | undefined;
+        const trackSector = (rail.node as Mesh).metadata?.trackSector as S5DogId | undefined;
+        const open = opts.openCam === id && trackFace === "B";
+        const missingOne = Boolean(opts.missingTrackPiece) && trackFace === "B" && trackSector === "PORT" && trackPiece === "CAM";
         const missingB =
           Boolean(opts.missingBFaces) &&
-          rail.name.startsWith("S5_LOCK_RAIL_B_PORT_") &&
-          !rail.name.endsWith("_MOUTH");
+          trackFace === "B" &&
+          trackSector === "PORT" &&
+          trackPiece !== "MOUTH";
         const missing = open || missingOne || missingB;
         rail.role = missing ? "diagnostic" : "physical";
         rail.node.setEnabled(!missing);
       }
-      const backs = solids.filter((s) => isSectorBacking(s.name, id));
+      const backingNames = id === "PORT" || id === "STARBOARD"
+        ? [`S5_LOCK_TRACK_BACK_${id}`]
+        : [
+            `S5_LOCK_TRACK_BACK_AFT_${id}`,
+            `S5_LOCK_TRACK_BACK_${id}`,
+            `S5_LOCK_TRACK_BACK_MOUTH_${id}`,
+            `S5_LOCK_TRACK_BACK_MOUTH_BRIDGE_${id}`,
+          ];
+      const backs = backingNames.map(authorityRow).filter(Boolean) as AuthoritySolid[];
       for (const back of backs) {
         const hide = Boolean(opts.trackFloat && id === "PORT");
         back.role = hide ? "diagnostic" : "physical";
         back.node.setEnabled(!hide);
       }
     }
-    const plug = solids.find((s) => s.name === "S5_CAN_PORT_GUIDE_PLUG_VIS");
+    const plug = authorityRow("S5_CAN_PORT_GUIDE_PLUG_VIS");
     if (plug) {
       const on = Boolean(opts.solidGuide);
       plug.role = on ? "physical" : "diagnostic";
       plug.node.setEnabled(on);
     }
-    const intrude = solids.find((s) => s.name === "S5_LOCK_RAIL_INTRUDE_PORT_VIS");
+    const intrude = authorityRow("S5_LOCK_RAIL_INTRUDE_PORT_VIS");
     if (intrude) {
       const on = Boolean(opts.railIntrude || opts.middlePathIntruder);
       intrude.role = on ? "physical" : "diagnostic";
@@ -687,7 +726,7 @@
       };
       intrude.node.setEnabled(on);
     }
-    const named = solids.find((s) => s.name === "S5_NC_TRACK_WEDGE_VIS");
+    const named = authorityRow("S5_NC_TRACK_WEDGE_VIS");
     if (named) {
       const on = Boolean(opts.namedIntrude);
       named.role = on ? "physical" : "diagnostic";
@@ -698,7 +737,7 @@
       };
       named.node.setEnabled(on);
     }
-    const backPlug = solids.find((s) => s.name === "S5_LOCK_BACKING_PLUG_PORT_VIS");
+    const backPlug = authorityRow("S5_LOCK_BACKING_PLUG_PORT_VIS");
     if (backPlug) {
       const on = Boolean(opts.backingPlug);
       backPlug.role = on ? "physical" : "diagnostic";
@@ -709,7 +748,7 @@
       };
       backPlug.node.setEnabled(on);
     }
-    const entryBlock = solids.find((s) => s.name === "S5_NC_ENTRY_MOUTH_BLOCK_VIS");
+    const entryBlock = authorityRow("S5_NC_ENTRY_MOUTH_BLOCK_VIS");
     if (entryBlock) {
       const on = Boolean(opts.entryBlock);
       entryBlock.role = on ? "physical" : "diagnostic";
@@ -720,7 +759,7 @@
       };
       entryBlock.node.setEnabled(on);
     }
-    const unclassified = solids.find((s) => s.name === "S5_NC_UNCLASSIFIED_INTRUDER_VIS");
+    const unclassified = authorityRow("S5_NC_UNCLASSIFIED_INTRUDER_VIS");
     if (unclassified) {
       const on = Boolean(opts.unclassifiedIntrude || opts.untaggedCorridorSolid);
       unclassified.role = on ? "physical" : "diagnostic";
@@ -735,23 +774,27 @@
       [movingPathIntruder, Boolean(opts.pathMovingUntagged)],
       [forgedFamilyIntruder, Boolean(opts.forgedSurroundingFamily)],
     ] as const) {
-      const spec = solids.find((candidate) => candidate.name === solid.name);
+      const spec = registeredSpecForMesh(solid, solids);
       if (spec) spec.role = on ? "physical" : "diagnostic";
       solid.metadata = { ...(solid.metadata ?? {}), assembly: undefined, subsystem: undefined };
       solid.setEnabled(on);
     }
-    const transientSpec = solids.find((candidate) => candidate.node === transientIntruder);
+    const transientSpec = authorityRow("S5_NC_MIDPATH_TRANSIENT_VIS");
     if (transientSpec) transientSpec.role = opts.midpathTransientRow ? "physical" : "diagnostic";
     transientIntruder.metadata = { ...(transientIntruder.metadata ?? {}), assembly: undefined, subsystem: undefined };
     transientIntruder.setEnabled(false);
 
     const duplicateNameOn = Boolean(opts.duplicateAuthorityName);
+    const duplicateNameSpec = authorityRow("S5_NC_DUPLICATE_AUTHORITY_NAME_VIS");
+    if (!duplicateNameSpec) throw new Error("duplicate-name authority fixture binding missing");
     duplicateNameSpec.role = duplicateNameOn ? "physical" : "diagnostic";
     duplicateNameSpec.name = duplicateNameOn ? "S5_LOCK_PIN_PORT" : "S5_NC_DUPLICATE_AUTHORITY_NAME_VIS";
     duplicateNameMesh.name = duplicateNameSpec.name;
     duplicateNameMesh.setEnabled(duplicateNameOn);
 
     const duplicateMovingOn = Boolean(opts.duplicateMovingRow);
+    const duplicateMovingSpec = authorityRow("S5_NC_DUPLICATE_MOVING_ROW_VIS");
+    if (!duplicateMovingSpec) throw new Error("duplicate-moving authority fixture binding missing");
     duplicateMovingSpec.role = duplicateMovingOn ? "physical" : "diagnostic";
     duplicateMovingMesh.setEnabled(duplicateMovingOn);
     applyLockPose(1, opts.forceUnlock ? "retract" : opts.lockGap ? "gap" : opts.forceLock ? "extend" : undefined);
@@ -767,7 +810,7 @@
     note: "S5 B′ propulsion architecture — fixed core + hollow can + handover",
   });
 
-  return { root, canPort, datums, solids, applyOverride, currentOverride: () => live, applyLockPose, pins };
+  return { root, canPort, datums, solids, applyOverride, bindAuthorityRows, currentOverride: () => live, applyLockPose, pins };
 }
 
 function addSegmentedCanWalls(
@@ -850,16 +893,6 @@
   return tagAssembly(shoe, "MOVING_LOCK_ASSEMBLY");
 }
 
-export function isSectorBacking(name: string, id: S5DogId): boolean {
-  return (
-    name === `S5_LOCK_TRACK_BACK_${id}` ||
-    name === `S5_LOCK_TRACK_BACK_OUT_${id}` ||
-    name === `S5_LOCK_TRACK_BACK_AFT_${id}` ||
-    name === `S5_LOCK_TRACK_BACK_MOUTH_${id}` ||
-    name === `S5_LOCK_TRACK_BACK_MOUTH_BRIDGE_${id}`
-  );
-}
-
 function addClosedTrack(
   scene: Scene,
   parent: TransformNode,
@@ -884,6 +917,7 @@
       assembly: "FIXED_TRACK_ASSEMBLY",
       subsystem: "S5_LOCK_TRACK_SUBSYSTEM",
       trackFace: "A",
+      trackSector: id,
       trackPiece: piece.kind,
       trackNormal: [piece.nx, piece.ny, piece.nz],
     };
@@ -904,6 +938,7 @@
       assembly: "FIXED_TRACK_ASSEMBLY",
       subsystem: "S5_LOCK_TRACK_SUBSYSTEM",
       trackFace: "B",
+      trackSector: id,
       trackPiece: piece.kind,
       trackNormal: [piece.nx, piece.ny, piece.nz],
     };
@@ -1192,7 +1227,7 @@
   }
   mesh.position.set(...pos);
   mesh.metadata = { ...(mesh.metadata ?? {}), size, kind: "box" };
-  const spec = solids.find((s) => s.name === mesh.name);
+  const spec = registeredSpecForMesh(mesh, solids);
   if (spec) {
     spec.localHalf = { x: size[0] / 2, y: size[1] / 2, z: size[2] / 2 };
   }
@@ -1205,7 +1240,7 @@
   size: [number, number, number],
   pos: [number, number, number],
 ): void {
-  const spec = solids.find((s) => s.name === mesh.name);
+  const spec = registeredSpecForMesh(mesh, solids);
   if (on) {
     resizeBox(mesh, solids, size, pos);
     mesh.setEnabled(true);
@@ -1216,3 +1251,9 @@
     if (spec) spec.role = "diagnostic";
   }
 }
+
+function registeredSpecForMesh(mesh: Mesh, solids: AuthoritySolid[]): AuthoritySolid | undefined {
+  const registrationId = (mesh.metadata as { authorityRegistrationId?: string } | undefined)?.authorityRegistrationId;
+  if (!registrationId) return undefined;
+  return solids.find((row) => registrationIdOf(row) === registrationId);
+}
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=test-results' '--exclude=*.zip' '--exclude=*.diff' '--exclude=*.Zone.Identifier' MT1-S5HR3R/src/verify/s5Capture.ts MT1-S5HR3R1/src/verify/s5Capture.ts
--- MT1-S5HR3R/src/verify/s5Capture.ts	2026-08-25 14:45:18.000000000 -0500
+++ MT1-S5HR3R1/src/verify/s5Capture.ts	2026-08-26 16:46:37.734353384 -0500
@@ -26,21 +26,26 @@
 import {
   auditS5AuthorityIdentity,
   getS5AuthorityIdentityBinding,
+  registrationIdOf,
   type S5AuthorityIdentityAudit,
   type S5AuthorityIdentityBinding,
   type S5RegisteredAuthoritySolid,
 } from "../machine/s5Identity";
-import { getSupersededBy, meshEnabled } from "../machine/s5Authority";
-import { isSectorBacking } from "../machine/s5Propulsion";
+import { isS5MaterialRegistration, meshEnabled } from "../machine/s5Authority";
 import type { MachineRig } from "../machine/types";
 import { obbOverlaps, obbSeparation, obbWorldAabb, type OBB } from "../math/obb";
 import { evaluateDriveReadiness } from "./capturePredicates";
 
-export function isS5Material(s: { name: string; role: string; node?: { isEnabled?: (c?: boolean) => boolean } }): boolean {
-  if (s.role !== "physical") return false;
-  if (getSupersededBy(s.name)) return false;
-  if (s.node && !meshEnabled(s.node)) return false;
-  return true;
+export function isS5Material(s: AuthoritySolid): boolean {
+  return isS5MaterialRegistration(registrationIdOf(s), s.role, meshEnabled(s.node));
+}
+
+interface S5ProofEdgeReport {
+  fromRegistrationId: string;
+  toRegistrationId: string;
+  from: string;
+  to: string;
+  sep: number;
 }
 
 export interface S5PathCertificate {
@@ -48,6 +53,7 @@
   samples: number;
   pairsEvaluated: number;
   firstHit: string;
+  firstHitRegistrationIds: string[];
   minPinBacking: number;
   minShoeBacking: number;
   minShoeRail: number;
@@ -101,11 +107,15 @@
     relevantRegistrationIds: string[];
     capturedRelevant: string[];
     pathAdded: string[];
+    pathAddedRegistrationIds: string[];
     approvedSurrounding: string[];
+    approvedSurroundingRegistrationIds: string[];
     unclassified: string[];
+    unclassifiedRegistrationIds: string[];
     invalidClassification: string[];
     conflictingClassification: string[];
     missingRequired: string[];
+    missingRequiredRegistrationIds: string[];
     collisionScope: string[];
     collisionScopeRegistrationIds: string[];
     lifetimeRows: S5AuthorityLifetimeRow[];
@@ -308,6 +318,53 @@
   ["S5_CAN_TOP_STBD_OUT", "s5-can"],
 ]);
 
+const HOST_CAN_SEMANTIC_NAMES: Record<S5DogId, readonly string[]> = {
+  PORT: [
+    "S5_CAN_PORT",
+    "S5_CAN_PORT_ABOVE",
+    "S5_CAN_PORT_BELOW",
+    "S5_CAN_PORT_MOUTH",
+    "S5_CAN_PORT_GUIDE_PLUG_VIS",
+  ],
+  STARBOARD: ["S5_CAN_STBD", "S5_CAN_STBD_ABOVE", "S5_CAN_STBD_BELOW", "S5_CAN_STBD_MOUTH"],
+  TOP_PORT: ["S5_CAN_TOP", "S5_CAN_TOP_MID", "S5_CAN_TOP_MOUTH", "S5_CAN_TOP_PORT_OUT", "S5_CAN_TOP_STBD_OUT"],
+  TOP_STARBOARD: ["S5_CAN_TOP", "S5_CAN_TOP_MID", "S5_CAN_TOP_MOUTH", "S5_CAN_TOP_PORT_OUT", "S5_CAN_TOP_STBD_OUT"],
+};
+
+export const S5_CAN_SEMANTIC_NAMES = Object.freeze([
+  "S5_CAN_PORT",
+  "S5_CAN_PORT_ABOVE",
+  "S5_CAN_PORT_BELOW",
+  "S5_CAN_PORT_MOUTH",
+  "S5_CAN_STBD",
+  "S5_CAN_STBD_ABOVE",
+  "S5_CAN_STBD_BELOW",
+  "S5_CAN_STBD_MOUTH",
+  "S5_CAN_TOP",
+  "S5_CAN_TOP_MID",
+  "S5_CAN_TOP_MOUTH",
+  "S5_CAN_TOP_PORT_OUT",
+  "S5_CAN_TOP_STBD_OUT",
+  "S5_CAN_BOT",
+] as const);
+
+const TRACK_BACKING_SEMANTIC_NAMES: Record<S5DogId, readonly string[]> = {
+  PORT: ["S5_LOCK_TRACK_BACK_PORT"],
+  STARBOARD: ["S5_LOCK_TRACK_BACK_STARBOARD"],
+  TOP_PORT: [
+    "S5_LOCK_TRACK_BACK_AFT_TOP_PORT",
+    "S5_LOCK_TRACK_BACK_TOP_PORT",
+    "S5_LOCK_TRACK_BACK_MOUTH_TOP_PORT",
+    "S5_LOCK_TRACK_BACK_MOUTH_BRIDGE_TOP_PORT",
+  ],
+  TOP_STARBOARD: [
+    "S5_LOCK_TRACK_BACK_AFT_TOP_STARBOARD",
+    "S5_LOCK_TRACK_BACK_TOP_STARBOARD",
+    "S5_LOCK_TRACK_BACK_MOUTH_TOP_STARBOARD",
+    "S5_LOCK_TRACK_BACK_MOUTH_BRIDGE_TOP_STARBOARD",
+  ],
+};
+
 const REQUIRED_MOVING_NAMES = new Set(
   S5_DOG_IDS.flatMap((id) => S5_REQUIRED_TRACK_TOPOLOGY[id].moving),
 );
@@ -330,6 +387,9 @@
   readonly requiredFixedIds: ReadonlySet<string>;
   readonly requiredRailIds: ReadonlySet<string>;
   readonly expectedSurroundingFamilyById: Readonly<Record<string, string>>;
+  readonly hostCanIds: Readonly<Record<S5DogId, ReadonlySet<string>>>;
+  readonly sectorBackingIds: Readonly<Record<S5DogId, ReadonlySet<string>>>;
+  readonly passageAllowedIds: ReadonlySet<string>;
   readonly ids: Readonly<Record<string, string>>;
 }
 
@@ -348,6 +408,19 @@
   const expectedSurroundingFamilyById = Object.freeze(
     Object.fromEntries([...EXPECTED_CORRIDOR_SURROUNDING].map(([name, family]) => [id(name), family])),
   );
+  const hostCanIds: Readonly<Record<S5DogId, ReadonlySet<string>>> = Object.freeze({
+    PORT: new Set(HOST_CAN_SEMANTIC_NAMES.PORT.map(id)),
+    STARBOARD: new Set(HOST_CAN_SEMANTIC_NAMES.STARBOARD.map(id)),
+    TOP_PORT: new Set(HOST_CAN_SEMANTIC_NAMES.TOP_PORT.map(id)),
+    TOP_STARBOARD: new Set(HOST_CAN_SEMANTIC_NAMES.TOP_STARBOARD.map(id)),
+  });
+  const sectorBackingIds: Readonly<Record<S5DogId, ReadonlySet<string>>> = Object.freeze({
+    PORT: new Set(TRACK_BACKING_SEMANTIC_NAMES.PORT.map(id)),
+    STARBOARD: new Set(TRACK_BACKING_SEMANTIC_NAMES.STARBOARD.map(id)),
+    TOP_PORT: new Set(TRACK_BACKING_SEMANTIC_NAMES.TOP_PORT.map(id)),
+    TOP_STARBOARD: new Set(TRACK_BACKING_SEMANTIC_NAMES.TOP_STARBOARD.map(id)),
+  });
+  const passageAllowedIds = new Set([...PASSAGE_OK, ...S5_CAN_SEMANTIC_NAMES].map(id));
   const binding = Object.freeze({
     authority,
     ids,
@@ -355,6 +428,9 @@
     requiredFixedIds: new Set([...REQUIRED_FIXED_NAMES].map(id)),
     requiredRailIds: new Set([...REQUIRED_RAIL_NAMES].map(id)),
     expectedSurroundingFamilyById,
+    hostCanIds,
+    sectorBackingIds,
+    passageAllowedIds,
   });
   proofIdentityBindings.set(rig, binding);
   return binding;
@@ -385,6 +461,15 @@
     .filter(Boolean) as S5WorldSolid[];
 }
 
+function boundAuthorityNode(
+  rig: MachineRig,
+  binding: S5ProofIdentityBinding,
+  semanticName: string,
+): AuthoritySolid["node"] | undefined {
+  const registrationId = binding.ids[semanticName];
+  return rig.solids.find((row) => registrationIdOf(row) === registrationId)?.node;
+}
+
 function instantiateIntentionalContactRelations(
   rig: MachineRig,
   world: S5WorldSolid[],
@@ -524,22 +609,35 @@
   const approvedSurrounding = lifetimeRows
     .filter((row) => row.classification === "expected-surrounding")
     .map((row) => row.name);
+  const approvedSurroundingRegistrationIds = lifetimeRows
+    .filter((row) => row.classification === "expected-surrounding")
+    .map((row) => row.registrationId);
   const unclassified = lifetimeRows.filter((row) => row.classification === "unclassified").map((row) => row.name);
+  const unclassifiedRegistrationIds = lifetimeRows
+    .filter((row) => row.classification === "unclassified")
+    .map((row) => row.registrationId);
   const invalidClassification = lifetimeRows.filter((row) => row.classification === "invalid").map((row) => row.name);
   const conflictingClassification = lifetimeRows.filter((row) => row.classification === "conflicting").map((row) => row.name);
 
   const missingRequired: string[] = [];
+  const missingRequiredRegistrationIds: string[] = [];
   if (identity.pass) {
     for (const id of S5_DOG_IDS) {
       const required = S5_REQUIRED_TRACK_TOPOLOGY[id];
       for (const name of required.moving) {
         const match = boundSolid(worldById, binding, name);
-        if (!match) missingRequired.push(name);
+        if (!match) {
+          missingRequired.push(name);
+          missingRequiredRegistrationIds.push(binding.ids[name]);
+        }
         else if (!isMovingLockSolid(match) || !inLockTrackSubsystem(match)) invalidClassification.push(name);
       }
       for (const name of [...required.faceA, ...required.faceB, ...required.support]) {
         const match = boundSolid(worldById, binding, name);
-        if (!match) missingRequired.push(name);
+        if (!match) {
+          missingRequired.push(name);
+          missingRequiredRegistrationIds.push(binding.ids[name]);
+        }
         else if (!isFixedTrackSolid(match) || !inLockTrackSubsystem(match)) invalidClassification.push(name);
       }
     }
@@ -555,6 +653,11 @@
   const pathAdded = uniq(
     lifetimeRows.filter((row) => !capturedRows.some((captured) => captured.registrationId === row.registrationId)).map((row) => row.name),
   );
+  const pathAddedRegistrationIds = uniq(
+    lifetimeRows
+      .filter((row) => !capturedRows.some((captured) => captured.registrationId === row.registrationId))
+      .map((row) => row.registrationId),
+  );
   const scopedRows = lifetimeRows.filter((row) => row.classification !== "required-moving-lock");
   const collisionScope = uniq(scopedRows.map((row) => row.name));
   const collisionScopeRegistrationIds = uniq(scopedRows.map((row) => row.registrationId));
@@ -580,11 +683,15 @@
     relevantRegistrationIds,
     capturedRelevant,
     pathAdded,
+    pathAddedRegistrationIds,
     approvedSurrounding: uniq(approvedSurrounding),
+    approvedSurroundingRegistrationIds: uniq(approvedSurroundingRegistrationIds),
     unclassified: uniq(unclassified),
+    unclassifiedRegistrationIds: uniq(unclassifiedRegistrationIds),
     invalidClassification: uniq(invalidClassification),
     conflictingClassification: uniq(conflictingClassification),
     missingRequired: uniq(missingRequired),
+    missingRequiredRegistrationIds: uniq(missingRequiredRegistrationIds),
     collisionScope,
     collisionScopeRegistrationIds,
     lifetimeRows: [...lifetimeRows].sort((a, b) => a.registrationId.localeCompare(b.registrationId)),
@@ -671,11 +778,11 @@
   allDogsCaptured: boolean;
   allLocksRetained: boolean;
   allLockDriversValid: boolean;
-  trackSupport: { pass: boolean; missing: string[]; edges: Array<{ from: string; to: string; sep: number }>; minContact: number };
+  trackSupport: { pass: boolean; missing: string[]; edges: S5ProofEdgeReport[]; minContact: number };
   fourSectorsDistinct: boolean;
   bottomSectorUsed: boolean;
-  seatGraph: { pass: boolean; missing: string[]; edges: Array<{ from: string; to: string; sep: number }> };
-  coreGraph: { pass: boolean; missing: string[]; edges: Array<{ from: string; to: string; sep: number }> };
+  seatGraph: { pass: boolean; missing: string[]; edges: S5ProofEdgeReport[] };
+  coreGraph: { pass: boolean; missing: string[]; edges: S5ProofEdgeReport[] };
   railBypass: boolean;
   driveThrustReady: boolean;
   firstApproachEvent: string;
@@ -685,6 +792,48 @@
   minLiveRetainedMargin: number;
 }
 
+function identityPreflightHandoverFailure(
+  rig: MachineRig,
+  identity: S5AuthorityIdentityAudit,
+): S5HandoverState {
+  const detail = `S5 identity preflight failed before handover projection duplicateNames=${identity.duplicateNames
+    .map((row) => `${row.name}[${row.registrationIds.join(",")}]`)
+    .join(";") || "none"} duplicateIds=${identity.duplicateRegistrationIds
+    .map((row) => `${row.registrationId}[${row.names.join(",")}]`)
+    .join(";") || "none"}`;
+  const failedGraph = { pass: false, missing: [detail], edges: [] as S5ProofEdgeReport[] };
+  return {
+    driveT: rig.lastDriveT(),
+    canMouthZ: 0,
+    insertion: 0,
+    insertionNondegenerate: false,
+    radialClearance: 0,
+    passageOpen: false,
+    passageBlockers: [detail],
+    registerSeated: false,
+    registerDetail: detail,
+    tongues: [],
+    dogs: [],
+    locks: [],
+    allTonguesSeated: false,
+    allDogsCaptured: false,
+    allLocksRetained: false,
+    allLockDriversValid: false,
+    trackSupport: { ...failedGraph, minContact: 0 },
+    fourSectorsDistinct: false,
+    bottomSectorUsed: false,
+    seatGraph: failedGraph,
+    coreGraph: failedGraph,
+    railBypass: false,
+    driveThrustReady: false,
+    firstApproachEvent: "identity_preflight_failed_before_projection",
+    liveSpigotLength: 0,
+    upstreamReady: false,
+    pathCertificateValid: false,
+    minLiveRetainedMargin: 0,
+  };
+}
+
 const LOCK_CHAIN: Array<[string, string]> = [
   ["S5_LOCK_PIN_PORT", "S5_LOCK_SHOULDER_PORT"],
   ["S5_LOCK_PIN_STARBOARD", "S5_LOCK_SHOULDER_STARBOARD"],
@@ -732,6 +881,42 @@
   ["BULKHEAD_Z-1p70", "VENTRAL_KEEL"],
 ];
 
+const NONPHYSICAL_CHAIN_TARGETS = new Set([
+  "AFT_POST_PORT",
+  "AFT_POST_STBD",
+  "BAY_WALL_PORT",
+  "BAY_WALL_STBD",
+  "BULKHEAD_Z-1p70",
+  "VENTRAL_KEEL",
+]);
+
+const LOCK_PIN_DOG_BY_SEMANTIC_NAME: Readonly<Record<string, S5DogId>> = Object.freeze(
+  Object.fromEntries(S5_DOG_IDS.map((id) => [`S5_LOCK_PIN_${id}`, id])),
+);
+
+interface S5ResolvedChainEdge {
+  aRegistrationId: string;
+  bRegistrationId: string;
+  aName: string;
+  bName: string;
+  bMayBeNonphysical: boolean;
+  retainedLockId?: S5DogId;
+}
+
+function resolveChainByRegistrationId(
+  binding: S5ProofIdentityBinding,
+  chain: ReadonlyArray<readonly [string, string]>,
+): S5ResolvedChainEdge[] {
+  return chain.map(([aName, bName]) => ({
+    aRegistrationId: binding.ids[aName],
+    bRegistrationId: binding.ids[bName],
+    aName,
+    bName,
+    bMayBeNonphysical: NONPHYSICAL_CHAIN_TARGETS.has(bName),
+    retainedLockId: LOCK_PIN_DOG_BY_SEMANTIC_NAME[aName],
+  }));
+}
+
 const PASSAGE_OK = new Set([
   "S5_SPIGOT_PORT",
   "S5_SPIGOT_STBD",
@@ -780,8 +965,7 @@
 export function liveCanMouthZ(rig: MachineRig, world: S5WorldSolid[]): number {
   const binding = getProofIdentityBinding(rig);
   const worldById = indexWorldByRegistrationId(world);
-  const canNames = Object.keys(binding.ids).filter((name) => name.startsWith("S5_CAN_"));
-  const walls = boundSolids(worldById, binding, canNames);
+  const walls = boundSolids(worldById, binding, S5_CAN_SEMANTIC_NAMES);
   if (walls.length === 0) return P.drive.stowedZ + P.drive.l / 2;
   let zFwd = -Infinity;
   for (const w of walls) {
@@ -792,6 +976,8 @@
 
 export function evaluateS5Handover(rig: MachineRig, _driveT = rig.lastDriveT()): S5HandoverState {
   void _driveT;
+  const identity = auditS5AuthorityIdentity(rig.solids);
+  if (!identity.pass) return identityPreflightHandoverFailure(rig, identity);
   const world = s5WorldOf(rig);
   const binding = getProofIdentityBinding(rig);
   const byRegistrationId = indexWorldByRegistrationId(world);
@@ -801,8 +987,8 @@
   const canMouthZ = liveCanMouthZ(rig, world);
   const tongues = S5_DOG_IDS.map((id) => evalTongue(rig, world, id));
   const locks = S5_DOG_IDS.map((id) => evalLock(rig, world, id));
-  const seatGraph = evalChain(byRegistrationId, binding, LOCK_CHAIN, locks);
-  const coreGraph = evalChain(byRegistrationId, binding, CORE_CHAIN);
+  const seatGraph = evalChain(byRegistrationId, resolveChainByRegistrationId(binding, LOCK_CHAIN), locks);
+  const coreGraph = evalChain(byRegistrationId, resolveChainByRegistrationId(binding, CORE_CHAIN));
   const bottom = Boolean(boundSolid(byRegistrationId, binding, "S5_DOG_BOTTOM_VIS"));
   const reg = evalRegister(rig, world, canMouthZ);
   const passage = evalPassage(rig, world, canMouthZ);
@@ -860,7 +1046,13 @@
     bottomSectorUsed: bottom,
     seatGraph,
     coreGraph,
-    railBypass: seatGraph.pass && !seatGraph.edges.some((e) => e.from.includes("RAIL") || e.to.includes("RAIL")),
+    railBypass:
+      seatGraph.pass &&
+      !seatGraph.edges.some(
+        (edge) =>
+          binding.requiredRailIds.has(edge.fromRegistrationId) ||
+          binding.requiredRailIds.has(edge.toRegistrationId),
+      ),
     driveThrustReady: ready,
     firstApproachEvent: "first_receiver_spigot_z_overlap",
     liveSpigotLength: spigot.length,
@@ -987,15 +1179,8 @@
 
 function hostCanWalls(rig: MachineRig, world: S5WorldSolid[], id: S5DogId): S5WorldSolid[] {
   const binding = getProofIdentityBinding(rig);
-  const worldById = indexWorldByRegistrationId(world);
-  const prefixes =
-    id === "PORT"
-      ? ["S5_CAN_PORT"]
-      : id === "STARBOARD"
-        ? ["S5_CAN_STBD"]
-        : ["S5_CAN_TOP"];
-  const names = Object.keys(binding.ids).filter((name) => prefixes.some((prefix) => name === prefix || name.startsWith(`${prefix}_`)));
-  return boundSolids(worldById, binding, names);
+  const ids = binding.hostCanIds[id];
+  return world.filter((row) => ids.has(row.registrationId) && isS5Material(row));
 }
 
 function pinExtensionU(id: S5DogId, pin: AuthoritySolid & { obb: OBB }): number {
@@ -1011,24 +1196,21 @@
   id: S5DogId,
 ): S5WorldSolid[] {
   const binding = getProofIdentityBinding(rig);
-  const worldById = indexWorldByRegistrationId(world);
-  const names = [
-    ...S5_REQUIRED_TRACK_TOPOLOGY[id].faceA,
-    ...S5_REQUIRED_TRACK_TOPOLOGY[id].faceB,
-    ...Object.keys(binding.ids).filter(
-      (name) => name.includes("INTRUDE") || name.includes("TRACK_WEDGE") || name.includes("BACKING_PLUG"),
-    ),
-  ];
-  return boundSolids(worldById, binding, names).filter(
-    (row) => binding.requiredRailIds.has(row.registrationId) || isFixedTrackSolid(row),
+  const sectorRailIds = new Set(
+    [...S5_REQUIRED_TRACK_TOPOLOGY[id].faceA, ...S5_REQUIRED_TRACK_TOPOLOGY[id].faceB]
+      .map((semanticName) => binding.ids[semanticName]),
+  );
+  return world.filter(
+    (row) =>
+      isS5Material(row) &&
+      (sectorRailIds.has(row.registrationId) || (isFixedTrackSolid(row) && inLockTrackSubsystem(row))),
   );
 }
 
 function sectorBackings(rig: MachineRig, world: S5WorldSolid[], id: S5DogId): S5WorldSolid[] {
   const binding = getProofIdentityBinding(rig);
-  const worldById = indexWorldByRegistrationId(world);
-  const names = Object.keys(binding.ids).filter((name) => isSectorBacking(name, id));
-  return boundSolids(worldById, binding, names);
+  const ids = binding.sectorBackingIds[id];
+  return world.filter((row) => ids.has(row.registrationId) && isS5Material(row));
 }
 
 function minSepToRails(follower: AuthoritySolid & { obb: OBB }, rails: Array<AuthoritySolid & { obb: OBB }>): number {
@@ -1058,12 +1240,12 @@
 function evalTrackSupport(rig: MachineRig, world: S5WorldSolid[]): {
   pass: boolean;
   missing: string[];
-  edges: Array<{ from: string; to: string; sep: number }>;
+  edges: S5ProofEdgeReport[];
   minContact: number;
 } {
   const binding = getProofIdentityBinding(rig);
   const worldById = indexWorldByRegistrationId(world);
-  const edges: Array<{ from: string; to: string; sep: number }> = [];
+  const edges: S5ProofEdgeReport[] = [];
   const missing: string[] = [];
   let minContact = Infinity;
   for (const id of S5_DOG_IDS) {
@@ -1077,14 +1259,20 @@
       missing.push(`BACK_${id}`);
       continue;
     }
-    const railBackings: string[] = [];
+    const railBackingIds: string[] = [];
     for (const rail of rails) {
       const seps = backs.map((back) => ({ back, sep: obbSeparation(rail.obb, back.obb) }));
       seps.sort((a, b) => a.sep - b.sep);
       const best = seps[0]!;
-      edges.push({ from: rail.name, to: best.back.name, sep: best.sep });
+      edges.push({
+        fromRegistrationId: rail.registrationId,
+        toRegistrationId: best.back.registrationId,
+        from: rail.name,
+        to: best.back.name,
+        sep: best.sep,
+      });
       if (best.sep > S5_CONTACT) missing.push(`${rail.name}↔backing sep=${best.sep.toFixed(4)}`);
-      else railBackings.push(best.back.name);
+      else railBackingIds.push(best.back.registrationId);
       minContact = Math.min(minContact, best.sep <= 0 ? -best.sep : 0);
     }
     const adjacency = new Map<string, Set<string>>();
@@ -1098,8 +1286,14 @@
       for (let j = i + 1; j < backs.length; j += 1) {
         const sep = obbSeparation(backs[i]!.obb, backs[j]!.obb);
         if (sep <= S5_CONTACT) {
-          edges.push({ from: backs[i]!.name, to: backs[j]!.name, sep });
-          connect(backs[i]!.name, backs[j]!.name);
+          edges.push({
+            fromRegistrationId: backs[i]!.registrationId,
+            toRegistrationId: backs[j]!.registrationId,
+            from: backs[i]!.name,
+            to: backs[j]!.name,
+            sep,
+          });
+          connect(backs[i]!.registrationId, backs[j]!.registrationId);
           minContact = Math.min(minContact, sep <= 0 ? -sep : 0);
         }
       }
@@ -1110,14 +1304,21 @@
       for (const back of backs) {
         const sep = obbSeparation(back.obb, post.obb);
         if (sep <= S5_CONTACT) {
-          edges.push({ from: back.name, to: post.name, sep });
-          connect(back.name, post.name);
+          edges.push({
+            fromRegistrationId: back.registrationId,
+            toRegistrationId: post.registrationId,
+            from: back.name,
+            to: post.name,
+            sep,
+          });
+          connect(back.registrationId, post.registrationId);
           minContact = Math.min(minContact, sep <= 0 ? -sep : 0);
         }
       }
-      for (const start of railBackings) {
-        const seen = new Set([start]);
-        const queue = [start];
+      for (const startRegistrationId of railBackingIds) {
+        const start = worldById.get(startRegistrationId);
+        const seen = new Set([startRegistrationId]);
+        const queue = [startRegistrationId];
         while (queue.length) {
           const next = queue.shift()!;
           for (const n of adjacency.get(next) ?? []) {
@@ -1127,7 +1328,9 @@
             }
           }
         }
-        if (!seen.has(post.name)) missing.push(`${start} has no backing→post path`);
+        if (!seen.has(post.registrationId)) {
+          missing.push(`${start?.name ?? startRegistrationId}[${startRegistrationId}] has no backing→post path`);
+        }
       }
     }
   }
@@ -1158,8 +1361,10 @@
   rig: MachineRig,
   id: S5DogId,
 ): { margin: number; stopDelta: number; blocked: boolean } {
-  const scene = rig.root.getScene();
-  const pin = scene.getMeshByName(`S5_LOCK_PIN_${id}`) as Mesh | null;
+  const identity = auditS5AuthorityIdentity(rig.solids);
+  if (!identity.pass) return { margin: 0, stopDelta: 0, blocked: false };
+  const binding = getProofIdentityBinding(rig);
+  const pin = boundAuthorityNode(rig, binding, `S5_LOCK_PIN_${id}`) as Mesh | undefined;
   if (!pin) return { margin: 0, stopDelta: 0, blocked: false };
   const sx = pin.position.x;
   const sy = pin.position.y;
@@ -1168,7 +1373,6 @@
     else if (id === "STARBOARD") pin.position.x = sx - d;
     else pin.position.y = sy - d;
     const world = s5WorldOf(rig);
-    const binding = getProofIdentityBinding(rig);
     const worldById = indexWorldByRegistrationId(world);
     const fol = boundSolid(worldById, binding, `S5_LOCK_CAM_SHOE_${id}`);
     const railsB = boundSolids(worldById, binding, S5_REQUIRED_TRACK_TOPOLOGY[id].faceB);
@@ -1284,17 +1488,6 @@
 
 function evalPassage(rig: MachineRig, world: S5WorldSolid[], mouthZ: number): { open: boolean; blockers: string[] } {
   const binding = getProofIdentityBinding(rig);
-  const passageAllowedIds = new Set(
-    Object.keys(binding.ids)
-      .filter(
-        (name) =>
-          PASSAGE_OK.has(name) ||
-          name.startsWith("S5_SPIGOT_") ||
-          name.startsWith("S5_RECEIVER_") ||
-          name.startsWith("S5_CAN_"),
-      )
-      .map((name) => binding.ids[name]),
-  );
   const corridor = {
     min: { x: -0.27, y: P.drive.y - 0.18, z: S5P.spigotZAft - 0.02 },
     max: { x: 0.27, y: P.drive.y + 0.18, z: mouthZ - P.drive.l + 0.05 },
@@ -1305,7 +1498,7 @@
     if (!isS5Material(s) && !(s.role === "physical" && meshEnabled(s.node))) continue;
     if (s.role !== "physical") continue;
     if (!meshEnabled(s.node)) continue;
-    if (passageAllowedIds.has(s.registrationId)) continue;
+    if (binding.passageAllowedIds.has(s.registrationId)) continue;
     const a = obbWorldAabb(s.obb);
     const hit =
       a.max.x > corridor.min.x &&
@@ -1321,33 +1514,44 @@
 
 function evalChain(
   byRegistrationId: ReadonlyMap<string, S5WorldSolid>,
-  binding: S5ProofIdentityBinding,
-  chain: Array<[string, string]>,
+  chain: readonly S5ResolvedChainEdge[],
   locks?: LockCapture[],
-): { pass: boolean; missing: string[]; edges: Array<{ from: string; to: string; sep: number }> } {
-  const edges: Array<{ from: string; to: string; sep: number }> = [];
+): { pass: boolean; missing: string[]; edges: S5ProofEdgeReport[] } {
+  const edges: S5ProofEdgeReport[] = [];
   const missing: string[] = [];
-  for (const [aName, bName] of chain) {
-    const a = boundSolid(byRegistrationId, binding, aName, false);
-    const b = boundSolid(byRegistrationId, binding, bName, false);
-    const bExempt = bName.startsWith("AFT_") || bName.startsWith("BAY_") || bName.startsWith("BULK") || bName.startsWith("VENTRAL");
-    if (!a || !b || a.role !== "physical" || (b.role !== "physical" && !bExempt)) {
-      missing.push(`${aName}↔${bName}`);
+  for (const edge of chain) {
+    const a = byRegistrationId.get(edge.aRegistrationId);
+    const b = byRegistrationId.get(edge.bRegistrationId);
+    if (!a || !b || a.role !== "physical" || (b.role !== "physical" && !edge.bMayBeNonphysical)) {
+      missing.push(`${edge.aName}[${edge.aRegistrationId}]↔${edge.bName}[${edge.bRegistrationId}]`);
       continue;
     }
-    if (aName.startsWith("S5_LOCK_PIN_")) {
-      const id = aName.replace("S5_LOCK_PIN_", "") as S5DogId;
-      const lock = locks?.find((l) => l.id === id);
+    if (edge.retainedLockId) {
+      const lock = locks?.find((row) => row.id === edge.retainedLockId);
       if (!lock?.retained) {
-        missing.push(`${aName}↔${bName} not retained`);
+        missing.push(`${edge.aName}[${edge.aRegistrationId}]↔${edge.bName}[${edge.bRegistrationId}] not retained`);
         continue;
       }
-      edges.push({ from: aName, to: bName, sep: -lock.lockTransverseInsertion });
+      edges.push({
+        fromRegistrationId: edge.aRegistrationId,
+        toRegistrationId: edge.bRegistrationId,
+        from: edge.aName,
+        to: edge.bName,
+        sep: -lock.lockTransverseInsertion,
+      });
       continue;
     }
     const sep = obbSeparation(a.obb, b.obb);
-    edges.push({ from: aName, to: bName, sep });
-    if (sep > S5_CONTACT) missing.push(`${aName}↔${bName} sep=${sep.toFixed(4)}`);
+    edges.push({
+      fromRegistrationId: edge.aRegistrationId,
+      toRegistrationId: edge.bRegistrationId,
+      from: edge.aName,
+      to: edge.bName,
+      sep,
+    });
+    if (sep > S5_CONTACT) {
+      missing.push(`${edge.aName}[${edge.aRegistrationId}]↔${edge.bName}[${edge.bRegistrationId}] sep=${sep.toFixed(4)}`);
+    }
   }
   return { pass: missing.length === 0, missing, edges };
 }
@@ -1370,6 +1574,8 @@
 }
 
 export function probeAxialEscape(rig: MachineRig, engaged?: boolean): EscapeProbe {
+  const identity = auditS5AuthorityIdentity(rig.solids);
+  if (!identity.pass) return { blocked: false, hits: ["identity preflight failed before axial-escape probe"] };
   const carriage =
     rig.root.getChildTransformNodes(false).find((n) => n.name === "DRIVE_CARRIAGE") ??
     rig.root.getScene().getTransformNodeByName("DRIVE_CARRIAGE");
@@ -1403,12 +1609,13 @@
   rig: MachineRig,
   id: S5DogId,
 ): { returnTravel: number; shoulderTravel: number; lead: number } {
-  const scene = rig.root.getScene();
-  const pin = scene.getMeshByName(`S5_LOCK_PIN_${id}`) as Mesh | null;
+  const identity = auditS5AuthorityIdentity(rig.solids);
+  if (!identity.pass) return { returnTravel: 1, shoulderTravel: 0, lead: -1 };
+  const binding = getProofIdentityBinding(rig);
+  const pin = boundAuthorityNode(rig, binding, `S5_LOCK_PIN_${id}`) as Mesh | undefined;
   if (!pin) return { returnTravel: 1, shoulderTravel: 0, lead: -1 };
   rig.applyDrive(1);
   const world0 = s5WorldOf(rig);
-  const binding = getProofIdentityBinding(rig);
   const world0ById = indexWorldByRegistrationId(world0);
   const pin0 = boundSolid(world0ById, binding, `S5_LOCK_PIN_${id}`, false);
   const sh = boundSolid(world0ById, binding, `S5_LOCK_SHOULDER_${id}`);
@@ -1485,6 +1692,7 @@
   const phases = new Set<string>();
   const internalHits: string[] = [];
   let firstHit = "";
+  let firstHitRegistrationIds: string[] = [];
   let pairsEvaluated = 0;
   let samples = 0;
   let minPinBacking = Infinity;
@@ -1498,7 +1706,142 @@
   const pathRelevantRows = new Map<string, S5AuthorityLifetimeRow>();
   const capturedRelevantIds = new Set<string>();
   const identity = auditS5AuthorityIdentity(rig.solids);
+  if (!identity.pass) {
+    const detail = `authority identity preflight failed before semantic/support/load/contact projection duplicateNames=${identity.duplicateNames
+      .map((row) => `${row.name}[${row.registrationIds.join(",")}]`)
+      .join(";") || "none"} duplicateIds=${identity.duplicateRegistrationIds
+      .map((row) => `${row.registrationId}[${row.names.join(",")}]`)
+      .join(";") || "none"}`;
+    const invalidReverse = Object.fromEntries(
+      S5_DOG_IDS.map((id) => [id, { returnContactTravel: -1, shoulderClearTravel: -1, shoulderConflictTravel: -1, lead: -1, clearInsertion: -1, tongueInsertionAtClear: -1, exitTravel: -1, finalExtension: 0 }]),
+    ) as S5PathCertificate["reverseShoulderClearanceLead"];
+    const invalidLead = Object.fromEntries(
+      S5_DOG_IDS.map((id) => [id, { returnTravel: -1, shoulderTravel: -1, lead: -1 }]),
+    ) as S5PathCertificate["reverseLead"];
+    const invalidRetained = Object.fromEntries(S5_DOG_IDS.map((id) => [id, -1])) as Record<S5DogId, number>;
+    const invalidEndpoint = Object.fromEntries(
+      S5_DOG_IDS.map((id) => [id, { extension: 0, insertion: -1, aSide: -1, bSide: -1 }]),
+    ) as S5PathCertificate["forwardEndpoint"];
+    const physicalRows = rig.solids.filter((row) => row.role === "physical" && meshEnabled(row.node));
+    const physicalIds = physicalRows.map(registrationIdOf);
+    const inventory: S5PathCertificate["inventory"] = {
+      method: "FULL_MOTION_SAMPLED_UNION",
+      proofIdentity: "registrationId",
+      identityPreflightPassed: false,
+      requiredTopologyProjected: false,
+      registeredRows: identity.registeredRows,
+      registrationIdsUnique: identity.duplicateRegistrationIds.length === 0 && identity.metadataMismatches.length === 0,
+      namesUnique: identity.duplicateNames.length === 0,
+      duplicateRegistrationIds: identity.duplicateRegistrationIds,
+      duplicateNames: identity.duplicateNames,
+      metadataIdentityMismatches: identity.metadataMismatches,
+      physicalUniverse: physicalRows.map((row) => row.name),
+      physicalUniverseRegistrationIds: physicalIds,
+      moving: [],
+      fixed: [],
+      relevant: [],
+      relevantRegistrationIds: [],
+      capturedRelevant: [],
+      pathAdded: [],
+      pathAddedRegistrationIds: [],
+      approvedSurrounding: [],
+      approvedSurroundingRegistrationIds: [],
+      unclassified: [],
+      unclassifiedRegistrationIds: [],
+      invalidClassification: [],
+      conflictingClassification: [],
+      missingRequired: ["AUTHORITY_IDENTITY_PREFLIGHT_FAILED_BEFORE_REQUIRED_TOPOLOGY_PROJECTION"],
+      missingRequiredRegistrationIds: [],
+      collisionScope: [],
+      collisionScopeRegistrationIds: [],
+      lifetimeRows: [],
+      complete: false,
+    };
+    const trackSupport = {
+      pass: false,
+      missing: ["AUTHORITY_IDENTITY_PREFLIGHT_FAILED_BEFORE_SUPPORT_PROJECTION"],
+      edges: [] as S5ProofEdgeReport[],
+      minContact: 0,
+    };
+    const pathCertificate: S5PathCertificate = {
+      valid: false,
+      samples: 0,
+      pairsEvaluated: 0,
+      firstHit: detail,
+      firstHitRegistrationIds: [],
+      minPinBacking: 0,
+      minShoeBacking: 0,
+      minShoeRail: 0,
+      reverseLead: invalidLead,
+      reverseShoulderClearanceLead: invalidReverse,
+      retainedWorst: invalidRetained,
+      passiveForward: false,
+      passiveReverse: false,
+      entryValid: false,
+      entryRange: { driveT0: forwardT0, driveT1: 1, sampleStep: dt, lastClearDriveT: forwardT0, firstContactDriveT: 1, lastSampledPreContactSeparation: 0 },
+      forwardEndpoint: invalidEndpoint,
+      requiredPieceTopology: S5_REQUIRED_TRACK_TOPOLOGY,
+      inventory,
+      pairProof: { identity: "registrationId", movingFixed: 0, movingSurrounding: 0, movingMoving: 0, foreignRail: 0, movingMovingPairs: [] },
+      intentionalContactTable: {
+        immutable: true,
+        rules: S5_INTENTIONAL_CONTACT_RULES,
+        relations: [],
+        instantiatedRelations: 0,
+        missingRelationRows: ["identity preflight failed before intentional-contact projection"],
+        pass: false,
+      },
+      analyticComparison: { maxDeviation: -1, tolerance: 2 * S5_TRACK_FREEPLAY_MAX + 0.00001, pass: false },
+      supportContinuity: false,
+      phases: ["IDENTITY_PREFLIGHT_FAILED_BEFORE_PROOF_PROJECTION"],
+      detail,
+    };
+    const drivers: LockDriver[] = S5_DOG_IDS.map((id) => ({
+      id,
+      valid: false,
+      region: "PRE_PICKUP",
+      guideAperture: false,
+      wallClear: false,
+      confined: false,
+      extendBlocked: false,
+      retractBlocked: false,
+      freePlay: 0,
+      minTrackClr: 0,
+      maxTrackClr: 0,
+      pickupDriveT: 1,
+      trackU: 0,
+      pinU: 0,
+      retainedMargin: -1,
+      trackSupported: false,
+      detail,
+    }));
+    const margins = Object.fromEntries(
+      S5_DOG_IDS.map((id) => [id, { margin: -1, stopDelta: -1, blocked: false }]),
+    ) as Record<S5DogId, { margin: number; stopDelta: number; blocked: boolean }>;
+    setPathCertificate(rig, pathCertificate);
+    return {
+      pass: false,
+      minCam: 0,
+      maxCam: 0,
+      minTrack: 0,
+      maxTrack: 0,
+      samples: 0,
+      drivers,
+      internal: { pass: false, hits: [detail], samples: 0 },
+      trackSupport,
+      margins,
+      pairsEvaluated: 0,
+      maxCenterlineError: -1,
+      firstHit: detail,
+      reverseLead: invalidLead,
+      pathCertificate,
+      detail,
+    };
+  }
   const proofBinding = getProofIdentityBinding(rig);
+  const pinNodes = Object.fromEntries(
+    S5_DOG_IDS.map((id) => [id, boundAuthorityNode(rig, proofBinding, `S5_LOCK_PIN_${id}`) as Mesh | undefined]),
+  ) as Record<S5DogId, Mesh | undefined>;
   const dogIdByMovingRegistrationId = new Map<string, S5DogId>(
     S5_DOG_IDS.flatMap((id) => [
       [proofBinding.ids[`S5_LOCK_PIN_${id}`], id] as const,
@@ -1515,7 +1858,10 @@
     t: number,
   ): void => {
     const hit = `${a.name}[${a.registrationId}]∩${b.name}[${b.registrationId}]@${stage}:${t.toFixed(5)} sep=${obbSeparation(a.obb, b.obb).toFixed(7)}`;
-    if (!firstHit) firstHit = hit;
+    if (!firstHit) {
+      firstHit = hit;
+      firstHitRegistrationIds = [a.registrationId, b.registrationId].sort();
+    }
     if (
       stage === "FORWARD" &&
       s5TrackBoundsAtZ(s5FollowerZ(t), Boolean(opts.reverseJam)).phase === "PRE_PICKUP" &&
@@ -1648,6 +1994,7 @@
       samples: 0,
       pairsEvaluated,
       firstHit,
+      firstHitRegistrationIds,
       minPinBacking: 0,
       minShoeBacking: 0,
       minShoeRail: 0,
@@ -1730,7 +2077,7 @@
   }
 
   const setExtension = (id: S5DogId, extension: number): void => {
-    const pin = rig.root.getScene().getMeshByName(`S5_LOCK_PIN_${id}`) as Mesh | null;
+    const pin = pinNodes[id];
     if (!pin) return;
     if (id === "PORT") pin.position.x = -S5_LOCK_PIN_RETRACT_X - extension;
     else if (id === "STARBOARD") pin.position.x = S5_LOCK_PIN_RETRACT_X + extension;
@@ -2054,6 +2401,7 @@
     samples,
     pairsEvaluated,
     firstHit,
+    firstHitRegistrationIds,
     minPinBacking: Number.isFinite(minPinBacking) ? minPinBacking : 0,
     minShoeBacking: Number.isFinite(minShoeBacking) ? minShoeBacking : 0,
     minShoeRail: Number.isFinite(minShoeRail) ? minShoeRail : 0,
@@ -2145,12 +2493,6 @@
   ];
 }
 
-export function s5FixedInventory(world: Array<{ name: string; role: string; moving: boolean }>): string[] {
-  return world
-    .filter((s) => s.name.startsWith("S5_") && s.role === "physical" && !s.moving && !s.name.includes("_VIS") && !s.name.includes("_NC_"))
-    .map((s) => s.name);
-}
-
 export function s5FixedNames(): string[] {
   return [
     "S5_CORE_PROXY",
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=test-results' '--exclude=*.zip' '--exclude=*.diff' '--exclude=*.Zone.Identifier' MT1-S5HR3R/src/verify/s5Clearance.ts MT1-S5HR3R1/src/verify/s5Clearance.ts
--- MT1-S5HR3R/src/verify/s5Clearance.ts	2026-08-25 14:45:38.000000000 -0500
+++ MT1-S5HR3R1/src/verify/s5Clearance.ts	2026-08-26 16:11:23.957753448 -0500
@@ -17,7 +17,7 @@
 } from "../design/s5Parameters";
 import { auditAuthorityBounds } from "../machine/authority";
 import { auditS5AuthorityIdentity, getS5AuthorityIdentityBinding, registrationIdOf } from "../machine/s5Identity";
-import { getS5Class, getSupersededBy, meshEnabled } from "../machine/s5Authority";
+import { getS5ClassByRegistrationId, meshEnabled } from "../machine/s5Authority";
 import type { S5MachineRig } from "../machine/createS5Machine";
 import type { GateResult, InstantEnvelope, MachineRig, SweptEnvelope } from "../machine/types";
 import { obbOverlaps, obbSeparation, obbWorldAabb, worldAabbUnion } from "../math/obb";
@@ -29,13 +29,14 @@
   isS5Material,
   markPathCertificateStale,
   probeAxialEscape,
+  S5_CAN_SEMANTIC_NAMES,
   s5MovingNames,
 } from "./s5Capture";
 import { runS4Authority } from "./s4Clearance";
 import { asAuditOptions, type MachineAuditOptions } from "./capturePredicates";
 import type { S5Override } from "../machine/s5Propulsion";
 
-const S5_REPAIR_ID = "MT1-S5HR3R";
+const S5_REPAIR_ID = "MT1-S5HR3R1";
 export const S5_C18_FROZEN_CLAIM =
   "C18 — Closed-World S5 Physical Certificate Completeness: For the frozen registered S5 physical universe and the explicitly declared S5 negative-control mutations, every authority row has a unique stable identity; every row that becomes physically relevant anywhere over the certified forward/reverse motion retains its identity and classification in the path certificate; every required collision/contact pair class is evaluated by identity, including moving-vs-moving where applicable; and no in-scope physical row can be omitted through duplicate names, transient lifetime, missing metadata, or broad family/name exemptions.";
 
@@ -152,15 +153,26 @@
   // Hold readiness explicitly absent so their applyMachine calls cannot trigger
   // duplicate path certificates.
   invalidatePathCertificate(rig);
+  const identityAudit = auditS5AuthorityIdentity(rig.solids);
   const s4 = ncActive ? null : runS4Authority(rig, auditOpts);
   const audit = auditAuthorityBounds(rig.root, rig.solids);
-  const identityAudit = auditS5AuthorityIdentity(rig.solids);
-  const identityBinding = getS5AuthorityIdentityBinding(rig);
-  const liveMat = rig.solids.filter((s) => isS5Material(s) || (s.role === "physical" && meshEnabled(s.node) && !getSupersededBy(s.name) && !s.name.startsWith("S5_NC_")));
-  const superseded = rig.solids.filter((s) => getS5Class(s.name) === "LEGACY_SUPERSEDED_REFERENCE" || s.name === "DRIVE_ENVELOPE" || s.name === "DRIVE_FACE_AFT");
-  const retained = rig.solids.filter((s) => getS5Class(s.name) === "RETAINED_S4A_PHYSICAL");
-  const neu = rig.solids.filter((s) => s.name.startsWith("S5_") && s.role === "physical" && meshEnabled(s.node));
-  const debugN = rig.solids.filter((s) => s.role === "diagnostic" || s.name.includes("_VIS"));
+  const identityBinding = identityAudit.pass ? getS5AuthorityIdentityBinding(rig) : undefined;
+  const liveMat = rig.solids.filter(isS5Material);
+  const superseded = rig.solids.filter(
+    (row) => getS5ClassByRegistrationId(registrationIdOf(row)) === "LEGACY_SUPERSEDED_REFERENCE",
+  );
+  const retained = rig.solids.filter(
+    (row) => getS5ClassByRegistrationId(registrationIdOf(row)) === "RETAINED_S4A_PHYSICAL",
+  );
+  const neu = rig.solids.filter(
+    (row) =>
+      getS5ClassByRegistrationId(registrationIdOf(row)) === "NEW_S5_PHYSICAL" &&
+      row.role === "physical" &&
+      meshEnabled(row.node),
+  );
+  const debugN = rig.solids.filter(
+    (row) => row.role === "diagnostic" || getS5ClassByRegistrationId(registrationIdOf(row)) === "DEBUG_NONPHYSICAL",
+  );
 
   const needEnv = !ncActive || Boolean(opts.envProtrusion);
   const needSweep = !ncActive || Boolean(opts.oldTopFrame || opts.blockReceiver || opts.backingPlug);
@@ -200,8 +212,12 @@
       opts.reverseJam,
   );
   const authorityRowsById = new Map(rig.solids.map((row) => [registrationIdOf(row), row]));
-  const envMesh = authorityRowsById.get(identityBinding.registrationIdBySemanticName.DRIVE_ENVELOPE);
-  const faceMesh = authorityRowsById.get(identityBinding.registrationIdBySemanticName.DRIVE_FACE_AFT);
+  const envMesh = identityBinding
+    ? authorityRowsById.get(identityBinding.registrationIdBySemanticName.DRIVE_ENVELOPE)
+    : undefined;
+  const faceMesh = identityBinding
+    ? authorityRowsById.get(identityBinding.registrationIdBySemanticName.DRIVE_FACE_AFT)
+    : undefined;
   const envActive = Boolean(envMesh && envMesh.role === "physical" && meshEnabled(envMesh.node));
   const faceActive = Boolean(faceMesh && faceMesh.role === "physical" && meshEnabled(faceMesh.node));
 
@@ -268,6 +284,19 @@
     g("S5-P16", "occupied-space authority", p16, env.detail),
   ];
 
+  const semanticRegistrationId = (semanticName: string): string =>
+    identityBinding?.registrationIdBySemanticName[semanticName] ?? "";
+  const missingPortBCamId = semanticRegistrationId("S5_LOCK_RAIL_B_PORT_CAM");
+  const missingPortBFaceIds = ["LEAD", "TRANSITION", "CAM_ENTRY", "CAM", "CAM_EXIT", "CAPTURE"].map(
+    (piece) => semanticRegistrationId(`S5_LOCK_RAIL_B_PORT_${piece}`),
+  );
+  const unclassifiedIntruderId = semanticRegistrationId("S5_NC_UNCLASSIFIED_INTRUDER_VIS");
+  const middlePathIntruderId = semanticRegistrationId("S5_LOCK_RAIL_INTRUDE_PORT_VIS");
+  const movingPathIntruderId = semanticRegistrationId("S5_NC_PATH_MOVING_UNTAGGED_VIS");
+  const forgedSurroundingId = semanticRegistrationId("S5_NC_FORGED_SURROUNDING_VIS");
+  const transientRowId = semanticRegistrationId("S5_NC_MIDPATH_TRANSIENT_VIS");
+  const duplicateMovingId = semanticRegistrationId("S5_NC_DUPLICATE_MOVING_ROW_VIS");
+
   const ncs: GateResult[] = [
     nc("NC1", "blocked can passage", Boolean(opts.blockPassage), Boolean(opts.blockPassage) && !p3, "P3 fails when plate inserted"),
     nc("NC2", "oversized core", Boolean(opts.oversizedCore), opts.oversizedCore ? !p4 : false, "P4 fails when core scaled"),
@@ -366,7 +395,7 @@
       Boolean(opts.missingTrackPiece),
       Boolean(opts.missingTrackPiece) &&
         !camTrack.pathCertificate.inventory.complete &&
-        camTrack.pathCertificate.inventory.missingRequired.includes("S5_LOCK_RAIL_B_PORT_CAM") &&
+        camTrack.pathCertificate.inventory.missingRequiredRegistrationIds.includes(missingPortBCamId) &&
         !p7 &&
         !p12 &&
         seated.driveThrustReady === false,
@@ -378,76 +407,78 @@
       Boolean(opts.missingBFaces),
       Boolean(opts.missingBFaces) &&
         !camTrack.pathCertificate.inventory.complete &&
-        camTrack.pathCertificate.inventory.missingRequired.filter((name) => name.startsWith("S5_LOCK_RAIL_B_PORT_")).length === 6 &&
+        missingPortBFaceIds.every((registrationId) =>
+          camTrack.pathCertificate.inventory.missingRequiredRegistrationIds.includes(registrationId),
+        ) &&
         !p7 &&
         !p12 &&
         seated.driveThrustReady === false,
-      `missingB=${camTrack.pathCertificate.inventory.missingRequired.filter((name) => name.startsWith("S5_LOCK_RAIL_B_PORT_")).join(",")} ready=${seated.driveThrustReady}`,
+      `missingB=${camTrack.pathCertificate.inventory.missingRequired.join(",")} ready=${seated.driveThrustReady}`,
     ),
     nc(
       "NC-UNTAGGED-CORRIDOR-SOLID",
       "enabled corridor obstruction with no lock metadata",
       Boolean(opts.untaggedCorridorSolid),
       Boolean(opts.untaggedCorridorSolid) &&
-        camTrack.pathCertificate.inventory.unclassified.includes("S5_NC_UNCLASSIFIED_INTRUDER_VIS") &&
+        camTrack.pathCertificate.inventory.unclassifiedRegistrationIds.includes(unclassifiedIntruderId) &&
         !camTrack.pathCertificate.inventory.complete &&
         !camTrack.pass &&
         !p12 &&
         seated.driveThrustReady === false,
-      `relevant=${camTrack.pathCertificate.inventory.relevant.includes("S5_NC_UNCLASSIFIED_INTRUDER_VIS")} unclassified=${camTrack.pathCertificate.inventory.unclassified.join(",")} ready=${seated.driveThrustReady}`,
+      `relevant=${camTrack.pathCertificate.inventory.relevantRegistrationIds.includes(unclassifiedIntruderId)} unclassified=${camTrack.pathCertificate.inventory.unclassified.join(",")} ready=${seated.driveThrustReady}`,
     ),
     nc(
       "NC-MIDDLE-PATH-INTRUDER",
       "endpoint-clear physical middle-path obstruction",
       Boolean(opts.middlePathIntruder),
       Boolean(opts.middlePathIntruder) && !camTrack.pass && !p7 && !p12 && seated.driveThrustReady === false,
-      `firstHit=${camTrack.firstHit} scope=${camTrack.pathCertificate.inventory.collisionScope.includes("S5_LOCK_RAIL_INTRUDE_PORT_VIS")} ready=${seated.driveThrustReady}`,
+      `firstHit=${camTrack.firstHit} scope=${camTrack.pathCertificate.inventory.collisionScopeRegistrationIds.includes(middlePathIntruderId)} ready=${seated.driveThrustReady}`,
     ),
     nc(
       "NC-PATH-MOVING-UNTAGGED",
       "moving untagged obstruction outside capture that crosses PORT A CAM earlier",
       Boolean(opts.pathMovingUntagged),
       Boolean(opts.pathMovingUntagged) &&
-        camTrack.pathCertificate.inventory.pathAdded.includes("S5_NC_PATH_MOVING_UNTAGGED_VIS") &&
-        camTrack.pathCertificate.inventory.unclassified.includes("S5_NC_PATH_MOVING_UNTAGGED_VIS") &&
-        camTrack.firstHit.includes("S5_NC_PATH_MOVING_UNTAGGED_VIS") &&
+        camTrack.pathCertificate.inventory.pathAddedRegistrationIds.includes(movingPathIntruderId) &&
+        camTrack.pathCertificate.inventory.unclassifiedRegistrationIds.includes(movingPathIntruderId) &&
+        camTrack.pathCertificate.firstHitRegistrationIds.includes(movingPathIntruderId) &&
         !camTrack.pathCertificate.inventory.complete &&
         !camTrack.pass &&
         !p7 &&
         !p12 &&
         seated.driveThrustReady === false,
-      `pathAdded=${camTrack.pathCertificate.inventory.pathAdded.includes("S5_NC_PATH_MOVING_UNTAGGED_VIS")} hit=${camTrack.firstHit} unclassified=${camTrack.pathCertificate.inventory.unclassified.join(",")} ready=${seated.driveThrustReady}`,
+      `pathAdded=${camTrack.pathCertificate.inventory.pathAddedRegistrationIds.includes(movingPathIntruderId)} hit=${camTrack.firstHit} unclassified=${camTrack.pathCertificate.inventory.unclassified.join(",")} ready=${seated.driveThrustReady}`,
     ),
     nc(
       "NC-FORGED-SURROUNDING-FAMILY",
       "rail obstruction with forged s5-can family but no exact surrounding identity",
       Boolean(opts.forgedSurroundingFamily),
       Boolean(opts.forgedSurroundingFamily) &&
-        camTrack.pathCertificate.inventory.relevant.includes("S5_NC_FORGED_SURROUNDING_VIS") &&
-        camTrack.pathCertificate.inventory.unclassified.includes("S5_NC_FORGED_SURROUNDING_VIS") &&
-        !camTrack.pathCertificate.inventory.approvedSurrounding.includes("S5_NC_FORGED_SURROUNDING_VIS") &&
-        camTrack.firstHit.includes("S5_NC_FORGED_SURROUNDING_VIS") &&
+        camTrack.pathCertificate.inventory.relevantRegistrationIds.includes(forgedSurroundingId) &&
+        camTrack.pathCertificate.inventory.unclassifiedRegistrationIds.includes(forgedSurroundingId) &&
+        !camTrack.pathCertificate.inventory.approvedSurroundingRegistrationIds.includes(forgedSurroundingId) &&
+        camTrack.pathCertificate.firstHitRegistrationIds.includes(forgedSurroundingId) &&
         !camTrack.pathCertificate.inventory.complete &&
         !camTrack.pass &&
         !p7 &&
         !p12 &&
         seated.driveThrustReady === false,
-      `relevant=${camTrack.pathCertificate.inventory.relevant.includes("S5_NC_FORGED_SURROUNDING_VIS")} approved=${camTrack.pathCertificate.inventory.approvedSurrounding.includes("S5_NC_FORGED_SURROUNDING_VIS")} hit=${camTrack.firstHit} ready=${seated.driveThrustReady}`,
+      `relevant=${camTrack.pathCertificate.inventory.relevantRegistrationIds.includes(forgedSurroundingId)} approved=${camTrack.pathCertificate.inventory.approvedSurroundingRegistrationIds.includes(forgedSurroundingId)} hit=${camTrack.firstHit} ready=${seated.driveThrustReady}`,
     ),
     nc(
       "NC-MIDPATH-TRANSIENT-ROW",
       "registration-ID retained for an enabled mid-path-only obstruction",
       Boolean(opts.midpathTransientRow),
       Boolean(opts.midpathTransientRow) &&
-        camTrack.pathCertificate.inventory.pathAdded.includes("S5_NC_MIDPATH_TRANSIENT_VIS") &&
+        camTrack.pathCertificate.inventory.pathAddedRegistrationIds.includes(transientRowId) &&
         camTrack.pathCertificate.inventory.lifetimeRows.some(
-          (row) => row.name === "S5_NC_MIDPATH_TRANSIENT_VIS" && row.firstRelevant.stage !== "PREREQUISITE",
+          (row) => row.registrationId === transientRowId && row.firstRelevant.stage !== "PREREQUISITE",
         ) &&
-        camTrack.firstHit.includes("S5_NC_MIDPATH_TRANSIENT_VIS") &&
+        camTrack.pathCertificate.firstHitRegistrationIds.includes(transientRowId) &&
         !camTrack.pathCertificate.inventory.complete &&
         !camTrack.pass &&
         seated.driveThrustReady === false,
-      `retained=${camTrack.pathCertificate.inventory.pathAdded.includes("S5_NC_MIDPATH_TRANSIENT_VIS")} hit=${camTrack.firstHit} ready=${seated.driveThrustReady}`,
+      `retained=${camTrack.pathCertificate.inventory.pathAddedRegistrationIds.includes(transientRowId)} hit=${camTrack.firstHit} ready=${seated.driveThrustReady}`,
     ),
     nc(
       "NC-DUPLICATE-AUTHORITY-NAME",
@@ -456,7 +487,7 @@
       Boolean(opts.duplicateAuthorityName) &&
         !camTrack.pathCertificate.inventory.namesUnique &&
         camTrack.pathCertificate.inventory.duplicateNames.some(
-          (row) => row.name === "S5_LOCK_PIN_PORT" && row.registrationIds.length === 2,
+          (row) => row.registrationIds.length === 2 && new Set(row.registrationIds).size === 2,
         ) &&
         !camTrack.pathCertificate.inventory.complete &&
         !camTrack.pass &&
@@ -469,7 +500,7 @@
       Boolean(opts.duplicateMovingRow),
       Boolean(opts.duplicateMovingRow) &&
         camTrack.pathCertificate.pairProof.movingMoving > 0 &&
-        camTrack.firstHit.includes("S5_NC_DUPLICATE_MOVING_ROW_VIS") &&
+        camTrack.pathCertificate.firstHitRegistrationIds.includes(duplicateMovingId) &&
         !camTrack.pathCertificate.inventory.complete &&
         !camTrack.pass &&
         seated.driveThrustReady === false,
@@ -810,10 +841,12 @@
   const binding = getS5AuthorityIdentityBinding(rig);
   const movingIds = new Set(s5MovingNames().map((name) => binding.registrationIdBySemanticName[name]));
   const s5Ids = new Set(
-    Object.entries(binding.registrationIdBySemanticName)
-      .filter(([semanticName]) => semanticName.startsWith("S5_"))
-      .map(([, registrationId]) => registrationId),
+    rig.solids
+      .filter((row) => getS5ClassByRegistrationId(registrationIdOf(row)) === "NEW_S5_PHYSICAL")
+      .map(registrationIdOf),
   );
+  const canIds = S5_CAN_SEMANTIC_NAMES.map((semanticName) => binding.registrationIdBySemanticName[semanticName]);
+  const canIdSet = new Set(canIds);
   const exactRulesByPair = new Map<string, Set<(typeof intentionalTable.relations)[number]["rule"]>>();
   for (const relation of intentionalTable.relations) {
     const pairId = [relation.aRegistrationId, relation.bRegistrationId].sort().join("↔");
@@ -846,10 +879,6 @@
       if (!core0) core0 = a;
       else if (Math.abs(a.min.z - core0.min.z) > 1e-6) coreFixed = false;
     }
-    const canIds = Object.entries(binding.registrationIdBySemanticName)
-      .filter(([semanticName]) => semanticName.startsWith("S5_CAN_"))
-      .map(([, registrationId]) => registrationId);
-    const canIdSet = new Set(canIds);
     const cans = world.filter((row) => canIdSet.has(registrationIdOf(row)) && isS5Material(row));
     if (env) {
       const ea = obbWorldAabb(env.obb);
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=test-results' '--exclude=*.zip' '--exclude=*.diff' '--exclude=*.Zone.Identifier' MT1-S5HR3R/tests/dump-s5-report.spec.ts MT1-S5HR3R1/tests/dump-s5-report.spec.ts
--- MT1-S5HR3R/tests/dump-s5-report.spec.ts	2026-08-25 14:45:01.000000000 -0500
+++ MT1-S5HR3R1/tests/dump-s5-report.spec.ts	2026-08-26 18:14:27.452839179 -0500
@@ -42,11 +42,11 @@
   contract("NC-PATH-MOVING-UNTAGGED", { pathMovingUntagged: true }, ["S5-P7", "S5-P12", "C18"], ["S5-P14", "C6", "C8", "C14", "C17", "C19", "C21", "C22", "C23", "C25"]),
   contract("NC-FORGED-SURROUNDING-FAMILY", { forgedSurroundingFamily: true }, ["S5-P7", "S5-P12", "C18"], ["S5-P14", "C6", "C8", "C14", "C17", "C19", "C21", "C22", "C23", "C25"]),
   contract("NC-MIDPATH-TRANSIENT-ROW", { midpathTransientRow: true }, ["C18"], ["S5-P7", "S5-P12", "S5-P14", "C6", "C8", "C14", "C15", "C17", "C19", "C21", "C22", "C23", "C25"]),
-  contract("NC-DUPLICATE-AUTHORITY-NAME", { duplicateAuthorityName: true }, ["C18"], ["S5-P7", "S5-P12", "S5-P14", "C6", "C8", "C14", "C15", "C17", "C19", "C21", "C22", "C23", "C25"]),
+  contract("NC-DUPLICATE-AUTHORITY-NAME", { duplicateAuthorityName: true }, ["C18"], ["S5-P3", "S5-P4", "S5-P5", "S5-P7", "S5-P8", "S5-P9", "S5-P10", "S5-P11", "S5-P12", "S5-P14", "C2", "C3", "C4", "C6", "C7", "C8", "C9", "C13", "C14", "C15", "C16", "C17", "C19", "C21", "C22", "C23", "C25"]),
   contract("NC-DUPLICATE-MOVING-ROW", { duplicateMovingRow: true }, ["C18"], ["S5-P7", "S5-P12", "S5-P14", "C6", "C8", "C14", "C15", "C17", "C19", "C21", "C22", "C23", "C25"]),
 ];
 
-test("@current-authority-writer dump MT1-S5HR3R authority report", async ({ page }) => {
+test("@current-authority-writer dump MT1-S5HR3R1 authority report", async ({ page }) => {
   test.setTimeout(600_000);
   await page.goto("/");
   await page.waitForFunction(() => Boolean(window.__MT1?.runS5Authority));
@@ -57,7 +57,7 @@
   expect(report.negativeControls.every((row) => row.status === "NOT_RUN")).toBe(true);
 });
 
-test("@evidence-writer dump strict S5HR3R negative-control ledger", async ({ page }) => {
+test("@evidence-writer dump strict S5HR3R1 negative-control ledger", async ({ page }) => {
   test.setTimeout(3_600_000);
   await page.goto("/");
   await page.waitForFunction(() => Boolean(window.__MT1?.runS5Authority));
@@ -65,7 +65,7 @@
   rmSync(evidenceTempFile, { force: true });
   const persistRows = () => {
     mkdirSync(dirname(evidenceFile), { recursive: true });
-    writeFileSync(evidenceTempFile, JSON.stringify({ schema: "MT1-S5HR3R-negative-control-ledger-v1", complete: false, controls: rows }, null, 2));
+    writeFileSync(evidenceTempFile, JSON.stringify({ schema: "MT1-S5HR3R1-negative-control-ledger-v1", complete: false, controls: rows }, null, 2));
   };
 
   const upstream = await page.evaluate(() => {
@@ -162,17 +162,17 @@
   });
   persistRows();
   expect(rows.every((row) => row.finalControlResult === "PASS" && (row.unexpectedFailures as string[]).length === 0), JSON.stringify(rows.filter((row) => row.finalControlResult !== "PASS" || (row.unexpectedFailures as string[]).length), null, 2)).toBe(true);
-  writeFileSync(evidenceTempFile, JSON.stringify({ schema: "MT1-S5HR3R-negative-control-ledger-v1", complete: true, controls: rows }, null, 2));
+  writeFileSync(evidenceTempFile, JSON.stringify({ schema: "MT1-S5HR3R1-negative-control-ledger-v1", complete: true, controls: rows }, null, 2));
   renameSync(evidenceTempFile, evidenceFile);
 });
 
-test("validate strict executed S5HR3R ledger", () => {
+test("validate strict executed S5HR3R1 ledger", () => {
   const ledger = JSON.parse(readFileSync(evidenceFile, "utf8")) as {
     schema: string;
     complete: boolean;
     controls: Array<{ mutationApplied: boolean; publicReadyExpectation: boolean | "UNCHANGED"; actualPublicReady: boolean; unexpectedFailures: string[]; baselineRestorationVerified: boolean; restorationReadiness: { raw: boolean; evaluate: boolean; getter: boolean; certificate: boolean }; finalControlResult: string }>;
   };
-  expect(ledger.schema).toBe("MT1-S5HR3R-negative-control-ledger-v1");
+  expect(ledger.schema).toBe("MT1-S5HR3R1-negative-control-ledger-v1");
   expect(ledger.complete).toBe(true);
   expect(existsSync(evidenceTempFile)).toBe(false);
   expect(ledger.controls).toHaveLength(28);
diff -ruN '--exclude=node_modules' '--exclude=dist' '--exclude=test-results' '--exclude=*.zip' '--exclude=*.diff' '--exclude=*.Zone.Identifier' MT1-S5HR3R/tests/mt1-s5h-mechanical.spec.ts MT1-S5HR3R1/tests/mt1-s5h-mechanical.spec.ts
--- MT1-S5HR3R/tests/mt1-s5h-mechanical.spec.ts	2026-08-25 14:35:49.000000000 -0500
+++ MT1-S5HR3R1/tests/mt1-s5h-mechanical.spec.ts	2026-08-26 16:11:24.092758300 -0500
@@ -3,16 +3,39 @@
 import { resolve } from "node:path";
 import type { S5Override } from "../src/machine/s5Propulsion";
 
-test.describe("MT1-S5HR3R frozen authority-contract path", () => {
+test.describe("MT1-S5HR3R1 frozen authority-contract path", () => {
   test("proof-semantic row selection has no live-name projection", () => {
     const capture = readFileSync(resolve(process.cwd(), "src/verify/s5Capture.ts"), "utf8");
     const clearance = readFileSync(resolve(process.cwd(), "src/verify/s5Clearance.ts"), "utf8");
+    const propulsion = readFileSync(resolve(process.cwd(), "src/machine/s5Propulsion.ts"), "utf8");
+    const authority = readFileSync(resolve(process.cwd(), "src/machine/s5Authority.ts"), "utf8");
     expect(capture).not.toMatch(/new Map\(world\.map\(\(s\) => \[s\.name/);
     expect(capture).not.toMatch(/\.find\(\(s\) => s\.name === `S5_/);
+    expect(capture).not.toContain("getMeshByName");
+    expect(capture).not.toContain("getSupersededBy(s.name)");
+    expect(capture).not.toContain("railBackings.push(best.back.name)");
+    expect(capture).not.toContain("connect(back.name, post.name)");
+    expect(capture).not.toContain("seen.has(post.name)");
+    expect(capture).toContain("railBackingIds.push(best.back.registrationId)");
+    expect(capture).toContain("connect(back.registrationId, post.registrationId)");
+    expect(capture).toContain("seen.has(post.registrationId)");
     expect(clearance).not.toContain("function intentionalPair(");
     expect(clearance).not.toMatch(/intentionalPair\(m\.name, f\.name\)/);
+    expect(clearance).not.toContain("getSupersededBy(s.name)");
+    expect(clearance).not.toContain("getS5Class(s.name)");
+    expect(clearance).toContain("getS5ClassByRegistrationId");
+    expect(propulsion).not.toMatch(/solids\.(find|filter)\([^\n]*\.name/);
+    expect(propulsion).toContain("registeredSpecForMesh");
+    expect(authority).toContain("getSupersededByRegistrationId");
+    expect(authority).toContain("getS5ClassByRegistrationId");
     expect(capture).toContain("indexWorldByRegistrationId");
     expect(capture).toContain("getProofIdentityBinding");
+    const preflight = capture.indexOf("const identity = auditS5AuthorityIdentity(rig.solids);", capture.indexOf("export function evalCamTrack"));
+    const projection = capture.indexOf("const proofBinding = getProofIdentityBinding(rig);", preflight);
+    const support = capture.indexOf("evalTrackSupport(rig, initialWorld)", preflight);
+    expect(preflight).toBeGreaterThan(0);
+    expect(projection).toBeGreaterThan(preflight);
+    expect(support).toBeGreaterThan(projection);
   });
   test("complete unilateral path and assembly pair sweep", async ({ page }) => {
     test.setTimeout(300_000);
@@ -38,6 +61,11 @@
     expect(path.pathCertificate.pairProof.identity).toBe("registrationId");
     expect(path.pathCertificate.pairProof.movingMoving).toBeGreaterThan(0);
     expect(path.pathCertificate.intentionalContactTable.pass).toBe(true);
+    expect(path.trackSupport.edges.length).toBeGreaterThan(0);
+    for (const edge of path.trackSupport.edges) {
+      expect(edge.fromRegistrationId).toMatch(/^AUTHORITY_ROW_/);
+      expect(edge.toRegistrationId).toMatch(/^AUTHORITY_ROW_/);
+    }
     expect(path.pathCertificate.inventory.physicalUniverse.length).toBeGreaterThan(300);
     expect(path.pathCertificate.inventory.relevant.length).toBeGreaterThanOrEqual(
       path.pathCertificate.inventory.capturedRelevant.length,
@@ -207,13 +235,14 @@
         const report = window.__MT1!.runS5Authority!(opts);
         window.__MT1!.setMachineT(1);
         window.__MT1!.applyS5Override!(opts);
-        const evaluator = window.__MT1!.evaluateS5Handover!().driveThrustReady;
+        const handover = window.__MT1!.evaluateS5Handover!();
+        const evaluator = handover.driveThrustReady;
         const raw = window.__MT1!.applyMachineRaw!(1).driveThrustReady;
         const getter = window.__MT1!.getDriveThrustReady!();
         const cert = window.__MT1!.getS5PathCertificate!()!;
         window.__MT1!.applyS5Override!({});
         const restored = window.__MT1!.getDriveThrustReady!();
-        return { report, evaluator, raw, getter, cert, restored };
+        return { report, handover, evaluator, raw, getter, cert, restored };
       };
       return {
         transient: run({ midpathTransientRow: true }),
@@ -251,6 +280,15 @@
     expect(result.duplicateName.cert.inventory.identityPreflightPassed).toBe(false);
     expect(result.duplicateName.cert.inventory.requiredTopologyProjected).toBe(false);
     expect(result.duplicateName.cert.samples).toBe(0);
+    expect(result.duplicateName.cert.pairsEvaluated).toBe(0);
+    expect(result.duplicateName.cert.supportContinuity).toBe(false);
+    expect(result.duplicateName.cert.phases).toEqual(["IDENTITY_PREFLIGHT_FAILED_BEFORE_PROOF_PROJECTION"]);
+    expect(result.duplicateName.cert.intentionalContactTable.instantiatedRelations).toBe(0);
+    expect(result.duplicateName.cert.intentionalContactTable.relations).toEqual([]);
+    expect(result.duplicateName.handover.trackSupport.edges).toEqual([]);
+    expect(result.duplicateName.handover.seatGraph.edges).toEqual([]);
+    expect(result.duplicateName.handover.coreGraph.edges).toEqual([]);
+    expect(result.duplicateName.handover.firstApproachEvent).toBe("identity_preflight_failed_before_projection");
     expect(result.duplicateName.cert.inventory.duplicateNames).toContainEqual(
       expect.objectContaining({ name: "S5_LOCK_PIN_PORT" }),
     );
