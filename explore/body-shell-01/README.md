# MT1-BODY-SHELL-01

Non-authoritative body-form exploration over the unchanged **MT1-S5HR3R1** mechanism.

```bash
cd /home/cmish/MECHA/MT1/explore/body-shell-01
npm install
npm run dev -- --port 5183
```

Use `BODY OFF / BODY ON` to compare the certified mechanism with the concept layer. Suggested director sequence:

1. `BODY SPREAD`
2. `BODY MID`
3. `BODY DRIVE`
4. `TOP`, `SIDE`, `FRONT`, `REAR`
5. `PROP` with BODY ON, then BODY OFF

See `BODY_SHELL_01_DESIGN_NOTE.md`. This package creates no authority, changes no mechanism transform, and makes no cockpit, intake, engine-internal, aero/CFD, or propulsion-performance claim.
