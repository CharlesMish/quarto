{
  "name": "mt1-body-shell-02-exploration",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "MT1-BODY-SHELL-02 — non-authoritative fuselage/frame presentation over MT1-S5HR3R1",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5184",
    "build": "tsc --noEmit -p tsconfig.app.json && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4184",
    "test": "playwright test tests/body-shell-02.spec.ts --workers=1",
    "test:evidence": "playwright test tests/dump-s5-report.spec.ts --workers=1 --grep @evidence-writer",
    "test:authority": "playwright test tests/dump-s5-report.spec.ts --workers=1 --grep @current-authority-writer",
    "capture": "playwright test tests/capture-proof.spec.ts",
    "capture:h1": "playwright test tests/capture-s5-h1-review.spec.ts --workers=1",
    "capture:body": "playwright test tests/capture-body-shell-02.spec.ts --workers=1"
  },
  "engines": {
    "node": ">=20.19"
  },
  "dependencies": {
    "@babylonjs/core": "^8.26.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.2"
  }
}
